# 🔧 ИСПРАВЛЕНИЕ КРИТИЧЕСКИХ ОШИБОК СБОРКИ

## Проблема 1: Отсутствует react-native-reanimated

```bash
cd bakery-app/bakery-mobile-app/bakery-app
npx expo install react-native-reanimated
```

## Проблема 2: Слишком длинные пути (>260 символов)

**РЕШЕНИЕ:** Переместить проект в корень диска!

```bash
# Скопируй проект сюда:
C:\bakery-app\
```

Вместо:
```
C:\Users\Administrator\Documents\bakery-app\bakery-mobile-app\bakery-app\
```

## Проблема 3: compileSdk 34 → 35

Обнови `android/app/build.gradle`:

```gradle
android {
    compileSdk 35  // было 34
    
    defaultConfig {
        targetSdk 35  // было 34
    }
}
```

## Проблема 4: Google Services package name

В Firebase Console добавь package name: `com.bakery.pavlova`

Или скачай новый `google-services.json` с правильным package name.

## ЧТО ДЕЛАТЬ СЕЙЧАС:

1. Установи react-native-reanimated
2. Перемести проект в `C:\bakery-app\`
3. Обнови compileSdk на 35
4. Проверь google-services.json

После этого запусти сборку заново!
