# 🔧 РЕШЕНИЕ ПРОБЛЕМЫ СБОРКИ APK

## ❌ ПРОБЛЕМА

Gradle не может собрать APK из-за конфликтов в конфигурации:
1. `reactAndroidLibs` не найден (кэш Gradle)
2. `react-native-async-storage` не найден (проблема автолинковки)
3. Firebase плагин конфликтует с settings.gradle

## ✅ РЕШЕНИЕ - СОБЕРИ В ANDROID STUDIO

Командная строка Gradle кэширует файлы и не видит изменения. 
**Android Studio** - это правильный способ собрать APK.

### 📋 ШАГ ЗА ШАГОМ

1. **Открой Android Studio**
   - Запусти Android Studio
   - File → Open
   - Выбери: `C:\Users\Administrator\Documents\bakery-app\bakery-mobile-app\bakery-app\android`

2. **Дождись Gradle Sync**
   - Android Studio автоматически синхронизирует проект
   - Подожди 2-5 минут
   - Внизу должно появиться "Gradle sync finished"

3. **Если ошибки при синхронизации:**
   - File → Invalidate Caches → Invalidate and Restart
   - Подожди перезапуска
   - Gradle Sync запустится снова

4. **Собери APK**
   - Build → Build Bundle(s) / APK(s) → Build APK(s)
   - Подожди 5-10 минут
   - Появится уведомление "APK(s) generated successfully"

5. **Найди APK**
   ```
   bakery-app/bakery-mobile-app/bakery-app/android/app/build/outputs/apk/release/app-release.apk
   ```

## 🎯 АЛЬТЕРНАТИВА - EXPO PREBUILD

Если Android Studio не работает:

```cmd
cd bakery-app\bakery-mobile-app\bakery-app
npx expo prebuild --platform android --clean
cd android
gradlew.bat assembleRelease
```

Это займёт 10-15 минут, но Expo сам настроит все файлы правильно.

## 📝 ЧТО БЫЛО СДЕЛАНО

- ✅ Исправлен settings.gradle (упрощён, убраны лишние includeBuild)
- ✅ Заменены `reactAndroidLibs` на жёсткие версии (2.5.0)
- ✅ Firebase временно отключён
- ✅ npm install выполнен

## ⚠️ ПОЧЕМУ НЕ РАБОТАЕТ КОМАНДНАЯ СТРОКА

1. **Gradle Daemon кэширует файлы** - даже после изменений использует старые версии
2. **Kiro IDE автоформатирование** - вернуло старые версии файлов
3. **Expo autolinking** - требует правильной настройки через prebuild

## 🚀 РЕКОМЕНДАЦИЯ

**Используй Android Studio** - это самый надёжный способ собрать APK для Expo проекта.

---
**Время сборки в Android Studio:** 5-10 минут  
**Размер APK:** ~50-80 MB
