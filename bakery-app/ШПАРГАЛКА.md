# 📝 ШПАРГАЛКА - БЫСТРЫЕ КОМАНДЫ

## 🚀 СБОРКА APK

```bash
cd bakery-app/bakery-mobile-app/bakery-app/android
./gradlew clean
./gradlew assembleRelease
```

APK: `android/app/build/outputs/apk/release/app-release.apk`

---

## 🔍 ЛОГИ

```bash
# Android логи
npx react-native log-android

# Или через adb
adb logcat | grep -i "bakery"
```

---

## 🧪 ТЕСТ DEEP LINK

```bash
adb shell am start -W -a android.intent.action.VIEW \
  -d "bakery-app://auth-callback?token=test&type=signup" \
  com.bakery.pavlova
```

---

## 📱 УСТАНОВКА APK

```bash
adb install android/app/build/outputs/apk/release/app-release.apk
```

---

## 🌐 ССЫЛКИ

### Supabase Dashboard
```
https://supabase.com/dashboard
```

### Netlify Drop
```
https://app.netlify.com/drop
```

### Google Cloud Console
```
https://console.cloud.google.com
```

---

## 🔑 ДАННЫЕ SUPABASE

```
URL: https://qkyhwdmhkoizxjazwnti.supabase.co
Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

## 📂 ВАЖНЫЕ ФАЙЛЫ

### Код
- `src/context/AuthContext.tsx` - авторизация
- `src/lib/supabase.ts` - подключение к Supabase
- `android/app/src/main/AndroidManifest.xml` - Deep Links

### Страницы
- `email-redirect.html` - редирект для email
- `admin-chat.html` - админка чата

### SQL
- `НАСТРОЙКА_OAUTH_ПОЛНАЯ.sql` - триггеры профилей
- `ИСПРАВЛЕНИЕ_АДМИНКИ_ЧАТА.sql` - доступ к чату

---

## ✅ БЫСТРАЯ ПРОВЕРКА

### 1. Код без ошибок?
```bash
cd bakery-app/bakery-mobile-app/bakery-app
npm run android
```

### 2. Deep Links работают?
```bash
adb shell am start -W -a android.intent.action.VIEW \
  -d "bakery-app://auth-callback" com.bakery.pavlova
```

### 3. Supabase подключён?
Открой `admin-chat.html` в браузере - должны появиться данные

---

## 🐛 ЧАСТЫЕ ПРОБЛЕМЫ

### Белый экран после email
→ Проверь `emailRedirectTo` в AuthContext.tsx

### Google Auth зависает
→ Проверь Redirect URLs в Supabase

### Админка не видит сообщения
→ Выполни SQL из `ИСПРАВЛЕНИЕ_АДМИНКИ_ЧАТА.sql`

### APK не собирается
→ `./gradlew clean` и попробуй снова

---

## 📞 КОМАНДЫ ADB

```bash
# Список устройств
adb devices

# Установка APK
adb install путь/к/файлу.apk

# Логи
adb logcat

# Очистка данных приложения
adb shell pm clear com.bakery.pavlova

# Запуск приложения
adb shell am start -n com.bakery.pavlova/.MainActivity
```

---

## 🎯 ПОРЯДОК ДЕЙСТВИЙ

1. Залей `email-redirect.html` на Netlify
2. Получи ссылку
3. Обнови `emailRedirectTo` в коде
4. Выполни SQL в Supabase
5. Настрой Redirect URLs
6. Собери APK
7. Тестируй!

---

## ✨ ГОТОВО!

Всё что нужно - в одном месте! 🚀
