# 🎉 Новые функции приложения

## Что добавлено

### 1. ✅ Система оплаты баллами
**100 баллов = 1₽**

#### Как работает:
- В корзине появляется переключатель "Списать баллы"
- Доступен только авторизованным пользователям с балансом ≥ 100
- Скидка рассчитывается автоматически
- Баллы списываются сразу при оформлении заказа
- Новые баллы начисляются при доставке (5% от суммы)

#### Пример:
```
Баланс: 1200 баллов
Сумма заказа: 500₽

Включаем "Списать баллы":
- Скидка: 12₽ (1200 баллов)
- К оплате: 488₽
- Списано: 1200 баллов
```

#### Где найти:
1. Откройте корзину
2. Добавьте товары
3. Прокрутите вниз
4. Включите переключатель "Списать баллы"
5. Оформите заказ

### 2. ✅ Глобальные переводы
Теперь язык меняется во **всём приложении**, а не только в профиле!

#### Переведены:
- ✅ Главная страница (приветствие, поиск, категории)
- ✅ Корзина (все тексты, кнопки, итоги)
- ✅ Профиль (меню, уровни лояльности)
- ✅ Избранное (заголовки, фильтры)
- ✅ Заказы (статусы, кнопки)

#### Поддерживаемые языки:
- 🇷🇺 Русский
- 🇬🇧 English
- 🇰🇿 Қазақша
- 🏴 Татарча
- 🇺🇿 O'zbek
- 🇦🇲 Հայերեն

#### Как сменить язык:
1. Откройте профиль
2. Нажмите "Язык"
3. Выберите нужный язык
4. Весь интерфейс обновится автоматически

## Технические детали

### Система баллов

#### Расчёт скидки
```typescript
// Максимальная скидка в рублях
const maxDiscount = Math.floor(bonusPoints / 100);

// Скидка не может быть больше суммы заказа
const discount = Math.min(maxDiscount, orderTotal);

// Сколько баллов спишется
const pointsToSpend = discount * 100;
```

#### Списание баллов
```typescript
// При оформлении заказа
if (usePoints && pointsToSpend > 0) {
  const newBalance = (profile.bonus_points || 0) - pointsToSpend;
  await updateProfile({ bonus_points: newBalance });
}
```

#### Начисление баллов
```typescript
// При доставке заказа (через 60 секунд)
const earnedPoints = Math.floor(finalTotal * 0.05); // 5%
const newPoints = currentPoints + earnedPoints;
await updateProfile({ bonus_points: newPoints });
```

### Переводы

#### Использование в компонентах
```typescript
import { useSettings } from '../../src/context/SettingsContext';

function MyComponent() {
  const { t } = useSettings();
  
  return (
    <View>
      <Text>{t('cart_title')}</Text>
      <Text>{t('home_search_placeholder')}</Text>
    </View>
  );
}
```

#### Добавление нового перевода
```typescript
// 1. В SettingsContext.tsx
export const translations = {
  ru: {
    new_key: 'Новый текст',
  },
  en: {
    new_key: 'New text',
  },
  // ... для всех языков
};

// 2. В компоненте
<Text>{t('new_key')}</Text>
```

## Изменённые файлы

### 1. `app/(tabs)/cart.tsx`
- Добавлен переключатель "Списать баллы"
- Добавлен расчёт скидки баллами
- Добавлено списание баллов при оформлении
- Применены переводы ко всем текстам

### 2. `app/(tabs)/home.tsx`
- Применены переводы к приветствию
- Переведены категории товаров
- Переведён поиск и бонусная карточка

### 3. `src/context/SettingsContext.tsx`
- Расширен словарь переводов
- Добавлены ключи для системы баллов
- Обновлена функция `t()` для корректной работы

### 4. База данных
Добавлено поле в таблицу `orders`:
```sql
ALTER TABLE orders 
ADD COLUMN points_spent INTEGER DEFAULT 0;
```

## Примеры использования

### Оплата баллами

**Шаг 1: Добавьте товары в корзину**
```
Круассан с шоколадом × 2 = 378₽
Синнабон классический × 1 = 215₽
Итого: 593₽
```

**Шаг 2: Включите "Списать баллы"**
```
💰 Списать баллы
Ваш баланс: 1200 ≈ 12₽         [ON]

✓ Будет списано: 1200 баллов (-12₽)
```

**Шаг 3: Оформите заказ**
```
Товары (3):           593₽
Скидка баллами:       -12₽
Доставка:             150₽
─────────────────────────────
К оплате:             731₽

Вы получите +36 баллов (5%)
```

**Результат:**
- Списано: 1200 баллов
- Оплачено: 731₽
- Получите: +36 баллов при доставке

### Смена языка

**Русский → English**
```
Корзина → Cart
Товары → Subtotal
Доставка → Delivery
Оформить → Checkout
```

**Русский → Қазақша**
```
Корзина → Себет
Товары → Тауарлар
Доставка → Жеткізу
Оформить → Рәсімдеу
```

## Документация

Подробная документация доступна в файлах:

### 📄 СИСТЕМА_ОПЛАТЫ_БАЛЛАМИ.md
- Как работает система баллов
- Расчёт скидки
- Примеры использования
- Технические детали

### 📄 ГЛОБАЛЬНЫЕ_ПЕРЕВОДЫ.md
- Структура переводов
- Где применены переводы
- Как добавить новый перевод
- Поддерживаемые языки

## Тестирование

### Система баллов

**Тест 1: Списание баллов**
1. Авторизуйтесь (test@test.com / test123)
2. Добавьте товары в корзину
3. Включите "Списать баллы"
4. Оформите заказ
5. Проверьте баланс в профиле

**Тест 2: Начисление баллов**
1. Оформите заказ
2. Подождите 60 секунд
3. Проверьте уведомления
4. Проверьте баланс в профиле

### Переводы

**Тест 1: Смена языка**
1. Откройте профиль
2. Нажмите "Язык"
3. Выберите English
4. Проверьте, что весь интерфейс на английском

**Тест 2: Сохранение выбора**
1. Смените язык на Қазақша
2. Перезагрузите приложение
3. Проверьте, что язык сохранился

## Результат

✅ Система оплаты баллами работает (100 баллов = 1₽)
✅ Глобальные переводы применены ко всему приложению
✅ 6 языков полностью поддерживаются
✅ Автоматический расчёт скидки
✅ Списание при оформлении, начисление при доставке
✅ Красивый UI с переключателем
✅ Переводы сохраняются между сессиями

## Что дальше?

### Возможные улучшения:
1. История транзакций баллов
2. Специальные предложения за баллы
3. Реферальная программа (пригласи друга - получи баллы)
4. Ежедневные задания за баллы
5. Бонусы за отзывы

### Дополнительные языки:
- Турецкий (tr)
- Арабский (ar)
- Китайский (zh)
- И другие по запросу

