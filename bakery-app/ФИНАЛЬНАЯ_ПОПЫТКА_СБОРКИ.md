# Финальная попытка сборки APK

## Что сделали
Применили "железобетонный" settings.gradle для Windows с использованием `settingsDir.parentFile`.

## Ключевые изменения
```groovy
// Вычисляем путь от папки android к node_modules
def npmRoot = new File(settingsDir.parentFile, "node_modules")

// Подключаем плагины через вычисленный путь
includeBuild(new File(npmRoot, "@react-native/gradle-plugin"))
includeBuild(new File(npmRoot, "expo-modules-autolinking"))
```

## Почему это должно сработать
- ✅ Используем `settingsDir` (папка android) вместо `rootDir`
- ✅ Явно вычисляем путь к node_modules через `.parentFile`
- ✅ Нет зависимости от команд `node --print`
- ✅ Абсолютные пути через `new File()`

## Если не сработает
План Б: **EAS Build** (облачная сборка Expo)

```bash
# Установить EAS CLI
npm install -g eas-cli

# Залогиниться
eas login

# Настроить сборку
eas build:configure

# Запустить сборку
eas build -p android --profile preview
```

Через 10-15 минут получишь готовый APK без мучений с Gradle на Windows.

## Статус
🔄 Сборка запущена с финальной конфигурацией...
