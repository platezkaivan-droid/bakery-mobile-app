# 🥐 Павлова Bakery App - Итоговый отчёт проекта

## 📱 О проекте
Мобильное приложение для пекарни "Павлова" с каталогом товаров, корзиной, системой заказов, чатом поддержки и push-уведомлениями.

---

## 🛠 Технологический стек

### Frontend (Мобильное приложение)
| Технология | Версия | Назначение |
|------------|--------|------------|
| **React Native** | 0.76.x | Основной фреймворк |
| **Expo** | SDK 52 | Платформа разработки |
| **Expo Router** | 4.x | Навигация (file-based routing) |
| **TypeScript** | 5.x | Типизация |
| **React Native Reanimated** | 3.x | Анимации |
| **Expo Image** | - | Оптимизированные изображения |
| **AsyncStorage** | - | Локальное хранилище |

### Backend & Database
| Технология | Назначение |
|------------|------------|
| **Supabase** | BaaS (Backend as a Service) |
| **PostgreSQL** | База данных (через Supabase) |
| **Supabase Auth** | Аутентификация (Email + Google OAuth) |
| **Supabase Realtime** | Realtime подписки для чата |
| **Row Level Security (RLS)** | Безопасность данных |

### Push-уведомления
| Технология | Назначение |
|------------|------------|
| **Firebase Cloud Messaging (FCM)** | Push-уведомления |
| **FCM v1 API** | Современный API для отправки |
| **@react-native-firebase/messaging** | Клиент FCM в приложении |

### Админ-панель
| Технология | Назначение |
|------------|------------|
| **Vanilla HTML/CSS/JS** | Простая статическая страница |
| **Supabase JS Client** | Работа с базой данных |
| **Web Crypto API** | Подпись JWT для FCM |
| **GitHub Pages** | Хостинг админ-панели |

### Сборка Android
| Технология | Назначение |
|------------|------------|
| **Gradle** | Система сборки |
| **Android Studio** | IDE для сборки APK |
| **ProGuard/R8** | Минификация и обфускация |

---

## 📊 Структура базы данных (Supabase)

### Таблицы:
```sql
-- Пользователи (встроенная auth.users)

-- Товары
products (
  id, name, description, price, image_url,
  category_id, is_available, created_at,
  name_ru, name_en, name_hy, name_tt,  -- мультиязычность
  description_ru, description_en, description_hy, description_tt
)

-- Категории
categories (id, name, slug, image_url)

-- Заказы
orders (
  id, user_id, status, total, delivery_address,
  phone, created_at, points_used, points_earned
)

-- Позиции заказа
order_items (id, order_id, product_id, quantity, price)

-- Избранное
favorites (id, user_id, product_id, created_at)

-- Баллы лояльности
user_points (user_id, points, total_earned, total_spent)

-- История баллов
points_history (id, user_id, amount, type, description, order_id)

-- Сообщения чата поддержки
support_messages (
  id, user_id, text, is_admin, created_at
)

-- FCM токены для push
user_fcm_tokens (
  id, user_id, fcm_token, device_info, created_at, updated_at
)

-- Сторис
stories (id, title, image_url, link, is_active, order_index)
```

---

## 🔐 Аутентификация

### Реализованные методы:
1. **Email + Password** - классическая регистрация/вход
2. **Google OAuth** - вход через Google аккаунт
3. **Email подтверждение** - через deep links

### Конфигурация:
- **Supabase Auth** для управления сессиями
- **Google Cloud Console** для OAuth credentials
- **Firebase** для SHA-1 сертификатов
- **Deep Links** для callback после OAuth

---

## 🌍 Мультиязычность

### Поддерживаемые языки:
- 🇷🇺 Русский (по умолчанию)
- 🇬🇧 English
- 🇦🇲 Հայերdelays (Армянский)
- 🏳️ Татарча (Татарский)

### Реализация:
- `SettingsContext` для хранения выбранного языка
- Переводы интерфейса в коде
- Переводы товаров в базе данных (поля name_ru, name_en и т.д.)

---

## 💬 Чат поддержки

### Архитектура:
```
[Пользователь в приложении]
        ↓ отправляет сообщение
[Supabase: support_messages]
        ↓ realtime подписка
[Админ-панель на GitHub Pages]
        ↓ отвечает
[Supabase: support_messages + FCM push]
        ↓
[Push-уведомление на телефон]
```

### Компоненты:
- `app/support.tsx` - экран чата в приложении
- `admin-panel/index.html` - веб-админка для операторов
- FCM v1 API - отправка push напрямую из браузера

---

## 📲 Push-уведомления

### Как работает:
1. При входе в приложение получаем FCM токен
2. Сохраняем токен в `user_fcm_tokens`
3. Админ отправляет сообщение → берём токен из базы
4. Отправляем push через FCM v1 API
5. Уведомление приходит на телефон

### Технические детали:
- Firebase Service Account для авторизации
- JWT подпись через Web Crypto API
- OAuth2 для получения access token
- FCM v1 REST API для отправки

---

## 🚧 Проблемы и решения

### 1. Сборка Android
| Проблема | Решение |
|----------|---------|
| Длинные пути Windows | Перенос проекта в `C:\bakery-app` |
| Expo modules не найдены | Правильная настройка `settings.gradle` |
| Firebase конфликты | Обновление зависимостей Gradle |
| ProGuard ошибки | Добавление правил в `proguard-rules.pro` |
| Java версия | Использование Java 17 |

### 2. Аутентификация
| Проблема | Решение |
|----------|---------|
| Google OAuth не работает | Настройка SHA-1 в Firebase |
| Deep links не открываются | Правильный `AndroidManifest.xml` |
| Callback не обрабатывается | GitHub Pages для redirect URL |

### 3. Push-уведомления
| Проблема | Решение |
|----------|---------|
| FCM Legacy API deprecated | Переход на FCM v1 API |
| CORS при отправке | Встроенная отправка в HTML |
| Токен не сохраняется | Проверка разрешений Android 13+ |

### 4. База данных
| Проблема | Решение |
|----------|---------|
| RLS блокирует запросы | Настройка политик безопасности |
| Realtime не работает | Включение replication для таблиц |

---

## 🔮 Планы на будущее

### Улучшение чата поддержки:

```
📋 Система очередей операторов
├── Новое сообщение → уведомление всем операторам
├── Первый оператор "берёт" чат → назначается ответственным
├── Остальные видят что чат занят
└── Статусы: ожидает / в работе / решён

👥 Роли и права
├── Супер-админ - полный доступ
├── Оператор - только чаты
├── Менеджер - заказы + статистика
└── Аналитик - только просмотр

📊 Аналитика чатов
├── Среднее время ответа
├── Количество решённых обращений
├── Рейтинг операторов
└── Популярные вопросы

💬 Улучшения UX чата
├── Отправка изображений
├── Голосовые сообщения
├── Шаблоны быстрых ответов
├── История переписки
└── Оценка качества поддержки
```

### Новые функции приложения:
```
🛒 Улучшения заказов
├── Онлайн-оплата (Stripe/YooKassa)
├── Отслеживание доставки на карте
├── Повторный заказ в 1 клик
└── Расписание доставки

🎁 Программа лояльности
├── Уровни (Bronze/Silver/Gold)
├── Персональные скидки
├── Реферальная программа
└── Дни рождения - бонусы

📱 UX улучшения
├── Тёмная тема
├── Виджеты на главный экран
├── Apple Watch / Wear OS
└── Offline режим
```

### Технические улучшения:
```
🔧 Инфраструктура
├── CI/CD (GitHub Actions)
├── Автоматические билды
├── Тестирование (Jest + Detox)
└── Мониторинг (Sentry)

📈 Масштабирование
├── CDN для изображений
├── Кэширование (Redis)
├── Оптимизация запросов
└── Lazy loading
```

---

## 📁 Структура проекта

```
bakery-mobile-app/
├── bakery-app/
│   ├── app/                    # Экраны (Expo Router)
│   │   ├── (tabs)/            # Табы навигации
│   │   │   ├── index.tsx      # Главная
│   │   │   ├── catalog.tsx    # Каталог
│   │   │   ├── cart.tsx       # Корзина
│   │   │   ├── favorites.tsx  # Избранное
│   │   │   └── profile.tsx    # Профиль
│   │   ├── auth/              # Авторизация
│   │   │   ├── login.tsx
│   │   │   └── register.tsx
│   │   ├── support.tsx        # Чат поддержки
│   │   └── _layout.tsx        # Корневой layout
│   │
│   ├── src/
│   │   ├── components/        # UI компоненты
│   │   ├── context/           # React Context
│   │   │   ├── AuthContext.tsx
│   │   │   ├── CartContext.tsx
│   │   │   └── SettingsContext.tsx
│   │   ├── hooks/             # Custom hooks
│   │   ├── services/          # API сервисы
│   │   └── utils/             # Утилиты
│   │
│   ├── android/               # Android native код
│   │   ├── app/
│   │   │   ├── build.gradle
│   │   │   ├── google-services.json
│   │   │   └── src/main/
│   │   │       ├── AndroidManifest.xml
│   │   │       └── res/       # Ресурсы (иконки)
│   │   └── release/
│   │       └── app-release.apk
│   │
│   ├── admin-panel/           # Веб-админка
│   │   └── index.html         # Чат поддержки для операторов
│   │
│   ├── supabase/
│   │   └── functions/         # Edge Functions
│   │       └── send-push/     # Push через Supabase
│   │
│   ├── assets/                # Изображения, шрифты
│   ├── app.json               # Конфиг Expo
│   ├── package.json           # Зависимости
│   └── tsconfig.json          # TypeScript конфиг
```

---

## 🔗 Ссылки

| Ресурс | URL |
|--------|-----|
| **GitHub репозиторий** | https://github.com/platezkaivan-droid/bakery-mobile-app |
| **Админ-панель** | https://platezkaivan-droid.github.io/bakery-mobile-app/bakery-app/admin-panel/ |
| **Supabase проект** | https://supabase.com/dashboard/project/qkyhwdmhkoizxjazwnti |
| **Firebase Console** | https://console.firebase.google.com/project/bakery-app-6452b |

---

## 📝 Команды для разработки

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npx expo start

# Сборка Android APK
cd android && ./gradlew assembleRelease

# Запуск локального сервера админки
node admin-server.js
```

---

## 👨‍💻 Авторы

Пискунов Иван Николаевич.

---

*Последнее обновление: Декабрь 2025*
