# 🚀 Следующие шаги по переносу дизайна

## ✅ Что уже сделано:

1. ✅ Склонированы все 6 репозиториев с дизайнами из Lovable
2. ✅ Обновлены цвета в `src/constants/colors.ts`
3. ✅ Создан план переноса

## 📋 Что нужно сделать:

### 1. Главный экран (Home) - ПРИОРИТЕТ 1
Файл: `bakery-app/app/(tabs)/home.tsx`

**Изменения:**
- Обновить header с градиентным аватаром
- Добавить градиент на бонусную карточку
- Улучшить карточки товаров (больше тени, скругления 24px)
- Добавить бейджи "NEW" и скидки на карточки
- Обновить цвета кнопок и акцентов

**Референс:** `temp-design-home/src/components/`

### 2. Корзина (Cart)
Файл: `bakery-app/app/(tabs)/orders.tsx` (или создать отдельный)

**Референс:** `temp-design-cart/src/`

### 3. История заказов (Orders)
Файл: `bakery-app/app/(tabs)/orders.tsx`

**Референс:** `temp-design-orders/src/`

### 4. Избранное (Favorites)
Файл: `bakery-app/app/(tabs)/favorites.tsx`

**Референс:** `temp-design-favorites/src/`

### 5. Отслеживание (Tracking)
Файл: `bakery-app/app/(tabs)/tracking.tsx`

**Референс:** `temp-design-tracking/src/`

### 6. Профиль (Profile)
Файл: Создать `bakery-app/app/(tabs)/profile.tsx`

**Референс:** `temp-design-profile/src/`

## 🎨 Ключевые улучшения дизайна:

1. **Тени**: Использовать более мягкие тени
   ```typescript
   shadowColor: '#000',
   shadowOffset: { width: 0, height: 8 },
   shadowOpacity: 0.12,
   shadowRadius: 24,
   elevation: 8,
   ```

2. **Скругления**: Увеличить до 20-24px
   ```typescript
   borderRadius: 24,
   ```

3. **Градиенты**: Использовать LinearGradient
   ```typescript
   import { LinearGradient } from 'expo-linear-gradient';
   
   <LinearGradient
     colors={['#FF6B35', '#FF8552', '#D2691E']}
     start={{ x: 0, y: 0 }}
     end={{ x: 1, y: 1 }}
   >
   ```

4. **Бейджи**: Добавить на карточки товаров
   - "NEW" - зеленый (#34C759)
   - Скидка "-24%" - красный (#FF3B30)

## 🔧 Технические детали:

### Установить зависимости (если нужно):
```bash
npm install expo-linear-gradient
```

### Структура компонентов:
```
src/components/
  ├── ProductCard.tsx (обновить)
  ├── PromoBanner.tsx (обновить с градиентом)
  ├── CategoryPill.tsx (создать)
  ├── StoryCircle.tsx (создать)
  └── OrderCard.tsx (создать)
```

## 📱 Тестирование:

После каждого обновления:
1. Перезагрузить приложение (R в терминале Expo)
2. Проверить на веб-версии
3. Проверить на мобильном устройстве через Expo Go

## 💡 Совет:

Начните с главного экрана (Home), так как он самый важный. После того как он будет готов, остальные экраны будут проще, так как многие компоненты переиспользуются.

Хотите, чтобы я начал обновлять главный экран прямо сейчас?
