# 📱 КАК СОБРАТЬ APK ПРАВИЛЬНО

## ⚠️ ВАЖНО!
**Закрой Android Studio!** Expo проекты собираются через Expo CLI, а не через Android Studio напрямую.

## 🚀 СПОСОБ 1: Быстрая сборка (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Открой терминал
```bash
cd bakery-app\bakery-mobile-app\bakery-app
```

### Шаг 2: Собери и запусти на устройстве
```bash
npx expo run:android
```

**Что произойдёт:**
- Автоматически соберётся APK
- Установится на подключённое устройство или эмулятор
- Запустится приложение

**Требования:**
- Подключённый Android телефон с включённой "Отладка по USB"
- ИЛИ запущенный Android эмулятор

### Шаг 3: Найди APK
После сборки APK будет здесь:
```
android\app\build\outputs\apk\debug\app-debug.apk
```

## 🎯 СПОСОБ 2: Сборка без установки

Если нужен только APK файл:

### Шаг 1: Создай Android проект
```bash
npx expo prebuild --platform android --clean
```

### Шаг 2: Собери APK
```bash
cd android
gradlew.bat assembleDebug
cd ..
```

### Шаг 3: Забери APK
```
android\app\build\outputs\apk\debug\app-debug.apk
```

## 📦 СПОСОБ 3: Production APK (для публикации)

### Вариант A: Через EAS Build (облачная сборка)

```bash
# Установи EAS CLI
npm install -g eas-cli

# Войди в аккаунт Expo
eas login

# Настрой проект
eas build:configure

# Собери APK
eas build --platform android --profile preview
```

**Плюсы:**
- Сборка в облаке
- Не нужен мощный компьютер
- Автоматическая подпись

### Вариант B: Локальная production сборка

```bash
# Создай release keystore
cd android\app
keytool -genkeypair -v -storetype PKCS12 -keystore my-release-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000

# Собери release APK
cd ..\..
npx expo run:android --variant release
```

## 🧪 СПОСОБ 4: Тестирование без сборки

Самый быстрый способ протестировать:

```bash
npx expo start
```

Затем:
1. Установи **Expo Go** на телефон из Google Play
2. Отсканируй QR-код в терминале
3. Приложение запустится в Expo Go

**Минусы:**
- Работает только с Expo Go
- Не все нативные функции доступны

## 🔧 Решение проблем

### Ошибка: "SDK location not found"
```bash
# Создай файл local.properties
echo sdk.dir=C:\\Users\\Administrator\\AppData\\Local\\Android\\Sdk > android\local.properties
```

### Ошибка: "Gradle build failed"
```bash
# Очисти кэш
cd android
gradlew.bat clean
cd ..

# Пересобери
npx expo run:android
```

### Ошибка: "No devices found"
```bash
# Проверь подключённые устройства
adb devices

# Если пусто - включи "Отладка по USB" на телефоне
# Или запусти эмулятор в Android Studio
```

## 📋 Чеклист перед сборкой

- [ ] Node.js установлен
- [ ] npm install выполнен
- [ ] Android SDK установлен
- [ ] Устройство подключено ИЛИ эмулятор запущен
- [ ] Отладка по USB включена (для физического устройства)

## 🎯 Рекомендация

**Для разработки и тестирования:**
```bash
npx expo run:android
```

**Для публикации в Google Play:**
```bash
eas build --platform android --profile production
```

## 📱 Установка APK на телефон

После сборки:

1. Скопируй `app-debug.apk` на телефон
2. Открой файл на телефоне
3. Разреши установку из неизвестных источников
4. Установи приложение

## ✅ Итог

**НЕ используй Android Studio для сборки Expo проектов!**

**Правильная команда:**
```bash
npx expo run:android
```

Это всё! 🚀
