# 🔍 Проверка логов для отладки заказов

## Что нужно проверить

### 1. Откройте консоль разработчика
В терминале где запущен `npx expo start` вы увидите все логи

### 2. Оформите заказ
Сразу должны появиться логи:
```
[ЗАКАЗ] Создан заказ #ABC123, ID: uuid-xxx. Доставка через 60 секунд...
```

### 3. Через 60 секунд
Должны появиться логи:
```
[ЗАКАЗ] Обновляем статус заказа #ABC123 на "delivered"...
[ЗАКАЗ] Order ID для обновления: uuid-xxx
[ЗАКАЗ] Статус обновлён на "delivered" [массив с данными]
[ЗАКАЗ] Триггер обновления списка заказов
[ЗАКАЗ] Начислено 50 бонусов. Новый баланс: 200
[ЗАКАЗ] Уведомления отправлены
```

## Возможные ошибки

### Ошибка 1: "Ошибка обновления статуса"
**Причина:** Нет прав на обновление таблицы `orders`
**Решение:** Проверьте RLS политики в Supabase

### Ошибка 2: "Order ID для обновления: undefined"
**Причина:** ID заказа не сохранился
**Решение:** Проверьте создание заказа

### Ошибка 3: Статус обновился, но заказ не исчез
**Причина:** Список заказов не обновляется
**Решение:** Проверьте что `loadOrders` вызывается каждую секунду

## Что должно произойти

### Сразу после оформления
1. ✅ Лог создания заказа
2. ✅ Заказ появляется в "Активных"
3. ✅ Таймер: "1 мин 0 сек"

### Через 60 секунд
1. ✅ Лог обновления статуса
2. ✅ Лог "Статус обновлён"
3. ✅ Лог "Триггер обновления"
4. ✅ Заказ исчезает из "Активных"
5. ✅ Заказ появляется в "Истории"
6. ✅ Автопереключение вкладки

## Команды для проверки БД

### Проверить статус заказа в БД
```sql
SELECT id, status, created_at 
FROM orders 
WHERE user_id = 'your-user-id'
ORDER BY created_at DESC
LIMIT 5;
```

### Проверить RLS политики
```sql
SELECT * FROM pg_policies 
WHERE tablename = 'orders';
```

## Исправления

### Сохранение переменных для setTimeout
Теперь используются сохранённые значения:
- `savedOrderId` - ID заказа
- `savedOrderNumber` - номер заказа
- `savedEarnedPoints` - бонусы
- `savedProfile` - профиль пользователя

Это гарантирует что значения доступны внутри `setTimeout`
