# ✅ Система уведомлений настроена

## Что сделано

### 1. Адаптация под тёмную/светлую тему
- ✅ Уведомления (всплывающие) адаптированы под текущую тему
- ✅ Страница истории уведомлений поддерживает обе темы
- ✅ Динамические цвета для фона, текста и теней

### 2. Красный badge на иконке уведомлений
- ✅ Добавлен счётчик непрочитанных уведомлений в header главной страницы
- ✅ Badge показывает количество (до 9, потом "9+")
- ✅ Автоматически обновляется при изменении статуса

### 3. История уведомлений
- ✅ Добавлен контекст `NotificationContext` с хранением истории
- ✅ Страница `/notifications` показывает все уведомления
- ✅ Возможность отметить как прочитанное
- ✅ Возможность удалить уведомление
- ✅ Кнопка "Прочитать все"

### 4. Автоматическое создание уведомлений
- ✅ При оформлении заказа создаётся уведомление "Заказ доставлен! 🎉"
- ✅ Уведомление сохраняется в истории
- ✅ Показывается красный badge на иконке

## Структура

### NotificationContext
```typescript
interface NotificationContextType {
  // Всплывающие уведомления (временные)
  notifications: Notification[];
  showNotification: (notification) => void;
  hideNotification: (id: string) => void;
  clearAll: () => void;
  
  // История уведомлений (постоянные)
  storedNotifications: StoredNotification[];
  unreadCount: number;
  addStoredNotification: (notification) => void;
  markAsRead: (id: string) => void;
  markAllAsRead: () => void;
  deleteStoredNotification: (id: string) => void;
}
```

### Типы уведомлений

**Всплывающие (временные):**
- `success` - Успешное действие (зелёный)
- `error` - Ошибка (красный)
- `warning` - Предупреждение (жёлтый)
- `info` - Информация (синий)

**История (постоянные):**
- `order` - Заказы (синий)
- `promo` - Акции (красный)
- `delivery` - Доставка (зелёный)
- `bonus` - Бонусы (жёлтый)
- `system` - Системные (серый)

## Использование

### Показать всплывающее уведомление
```typescript
const { showNotification } = useNotification();

showNotification({
  type: 'success',
  title: 'Заказ оформлен!',
  message: 'Ожидайте доставку',
  duration: 3000, // опционально
});
```

### Добавить в историю
```typescript
const { addStoredNotification } = useNotification();

addStoredNotification({
  type: 'delivery',
  title: 'Заказ доставлен! 🎉',
  message: 'Ваш заказ #24891 успешно доставлен',
});
```

### Получить количество непрочитанных
```typescript
const { unreadCount } = useNotification();

// Показать badge если есть непрочитанные
{unreadCount > 0 && (
  <View style={styles.badge}>
    <Text>{unreadCount}</Text>
  </View>
)}
```

## Изменённые файлы

1. **src/context/NotificationContext.tsx**
   - Добавлена поддержка тем
   - Добавлено хранение истории уведомлений
   - Добавлены методы для работы с историей

2. **app/notifications.tsx**
   - Интеграция с контекстом
   - Адаптация под тёмную/светлую тему
   - Отображение истории из контекста

3. **app/(tabs)/home.tsx**
   - Добавлен badge с количеством непрочитанных
   - Интеграция с `unreadCount` из контекста

4. **app/(tabs)/cart.tsx**
   - Автоматическое создание уведомления при оформлении заказа
   - Сохранение в историю

## Особенности

### Тёмная тема
- Более контрастные тени для уведомлений
- Адаптированные цвета фона и текста
- Плавные переходы между темами

### Светлая тема
- Мягкие тени
- Светлые цвета фона
- Высокая читаемость

### Badge на иконке
- Красный цвет для привлечения внимания
- Компактный размер
- Показывает "9+" если больше 9 уведомлений

## Результат

✅ Уведомления красиво выглядят в обеих темах
✅ Красный badge показывает количество непрочитанных
✅ История уведомлений сохраняется
✅ При оформлении заказа автоматически создаётся уведомление
✅ Пользователь видит все важные события в одном месте
