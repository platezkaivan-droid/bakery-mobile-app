# 🎯 БЫСТРОЕ ИСПРАВЛЕНИЕ КАРТИНОК

## Проблема
В базе данных хранятся пути `/products/12.jpg`, а код ожидает ключи `eclair`, `strudel` и т.д.

## Решение за 2 минуты

### Шаг 1: Откройте Supabase
1. Перейдите на https://supabase.com
2. Откройте ваш проект
3. Перейдите в **SQL Editor**

### Шаг 2: Выполните SQL-скрипт
1. Откройте файл `ПОЛНОЕ_ИСПРАВЛЕНИЕ_КАРТИНОК.sql`
2. Скопируйте весь код
3. Вставьте в SQL Editor
4. Нажмите **Run** (или F5)

### Шаг 3: Проверьте результат
В конце скрипта будет показана таблица с результатами.
Все товары должны иметь правильные ключи без путей `/products/`.

### Шаг 4: Перезагрузите приложение
В терминале Expo нажмите `r`

---

## ✅ Что будет исправлено

| Было | Станет |
|------|--------|
| `/products/12.jpg` | `eclair` |
| `/products/24.jpg` | `strudel` |
| `/products/23.jpg` | `cremeBrulee` |
| `/products/1.jpg` | `croissant` |
| И т.д. | Правильные ключи |

---

## 📋 Полный список ключей

Все эти ключи уже есть в коде `favorites.tsx`:

- `croissant` - Круассан с шоколадом
- `cinnabon` - Синнабон
- `almondCroissant` - Круассан с миндалём
- `danish` - Датская булочка
- `donut` - Пончик
- `cinnamonBun` - Булочка с корицей
- `napoleon` - Наполеон
- `medovik` - Медовик
- `redVelvet` - Красный бархат
- `cheesecake` - Чизкейк
- `chocolateCake` - Шоколадный торт
- `eclair` - Эклер ✅
- `macarons` - Макаронс
- `tiramisu` - Тирамису
- `profiterole` - Профитроли
- `cupcake` - Капкейк
- `baguette` - Багет
- `ciabatta` - Чиабатта
- `ryeBread` - Ржаной хлеб
- `focaccia` - Фокачча
- `berryTart` - Тарт с ягодами
- `pannaCotta` - Панна котта
- `cremeBrulee` - Крем-брюле ✅
- `strudel` - Штрудель ✅

---

## 🔍 Если что-то не работает

### Проверьте логи в терминале
После перезагрузки приложения смотрите строки:
```
🖼️ Товар: Эклер с кремом | Ключ картинки: eclair
```

Если ключ правильный (`eclair`, а не `/products/12.jpg`) - всё ОК!

### Если картинка всё равно не показывается
Убедитесь что файл существует:
```
bakery-app/bakery-mobile-app/bakery-app/assets/products/ekler s cremom.jpg
```

---

## ⚡ Быстрый SQL (если лень копировать весь файл)

Минимальный скрипт для проблемных товаров:

\`\`\`sql
-- Исправляем только те, что на скриншоте
UPDATE products SET image_url = 'eclair' WHERE name LIKE '%клер%';
UPDATE products SET image_url = 'strudel' WHERE name LIKE '%трудель%';
UPDATE products SET image_url = 'cremeBrulee' WHERE name LIKE '%рем%брюле%';

-- Проверяем
SELECT name, image_url FROM products WHERE name LIKE '%клер%' OR name LIKE '%трудель%' OR name LIKE '%рем%брюле%';
\`\`\`

---

## 📅 Дата создания
${new Date().toLocaleString('ru-RU')}
