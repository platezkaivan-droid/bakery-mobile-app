# ⚡ СРОЧНО! Выполните эти 2 шага

## Проблема найдена!

Старый SQL использовал **несуществующие UUID**. Он выполнялся без ошибок, но обновлял **0 строк**.

---

## ✅ Решение (2 минуты)

### 1️⃣ Откройте Supabase SQL Editor
https://supabase.com → ваш проект → **SQL Editor**

### 2️⃣ Выполните НОВЫЙ SQL
Откройте файл: **`ПРАВИЛЬНЫЙ_SQL_ПЕРЕВОДЫ.sql`**

Скопируйте **весь** код → Вставьте → **RUN**

### 3️⃣ Проверьте
```sql
SELECT name, name_en FROM products LIMIT 5;
```

Должно быть **заполнено**:
```
Круассан с шоколадом | Chocolate Croissant ✅
Синнабон классический | Classic Cinnamon Roll ✅
```

### 4️⃣ Перезапустите
```bash
r
```

### 5️⃣ Тестируйте
Профиль → Язык → English → Главная

**Названия должны измениться!** 🎉

---

## Что изменилось?

### ❌ Старый SQL (не работал)
```sql
WHERE id = '00000000-0000-0000-0000-000000000001'
```
Искал по несуществующим UUID → обновлял 0 строк

### ✅ Новый SQL (работает)
```sql
WHERE name LIKE '%руассан%шоколад%'
```
Ищет по тексту названия → обновляет реальные товары

---

## Если не работает

### Колонка называется 'title'?
Замените в SQL все `name LIKE` на `title LIKE`

### Колонок name_en нет?
Выполните сначала:
```sql
ALTER TABLE products 
ADD COLUMN IF NOT EXISTS name_en TEXT,
ADD COLUMN IF NOT EXISTS name_kz TEXT;
```

---

**Подробности:** `ИСПРАВЛЕНИЕ_ДВУХ_ОШИБОК.md`
