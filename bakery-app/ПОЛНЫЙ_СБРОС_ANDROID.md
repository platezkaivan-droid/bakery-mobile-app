# Полный сброс Android папки

## Проблема
Множество ручных правок в build.gradle и settings.gradle привели к конфликтам версий плагинов Expo и Android.

## Решение
Используем `expo prebuild --clean` для полного пересоздания папки android с правильными настройками.

## Что делаем

### Шаг 1: Проверили app.json ✅
```json
{
  "expo": {
    "scheme": "bakery-app",  // ← Схема для Deep Links сохранена
    "android": {
      "package": "com.bakery.pavlova",
      "intentFilters": [...]  // ← Intent фильтры настроены
    }
  }
}
```

### Шаг 2: Запускаем prebuild 🔄
```bash
npx expo prebuild --clean --platform android
```

Эта команда:
- Удалит папку android
- Создаст её заново на основе app.json
- Правильно настроит все Gradle плагины
- Подключит все Expo модули через autolinking
- Применит intentFilters из app.json

### Шаг 3: После prebuild
Проверим AndroidManifest.xml - Deep Links должны быть там автоматически из app.json.

### Шаг 4: Соберём APK
```bash
cd android
./gradlew clean assembleRelease
```

## Почему это работает
Expo Prebuild знает правильные версии всех плагинов и зависимостей. Он создаст чистую конфигурацию без конфликтов.

## Статус
🔄 Prebuild запущен...
