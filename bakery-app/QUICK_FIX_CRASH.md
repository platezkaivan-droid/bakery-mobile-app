# Быстрое исправление краша при входе/регистрации

## ✅ Что сделано

1. **Отключен minification** - это была основная причина краша
   - `android.enableMinifyInReleaseBuilds=false`
   - `android.enableShrinkResourcesInReleaseBuilds=false`

2. **Добавлены ProGuard правила** - на будущее, когда включим minification обратно
   - Правила для Supabase, AsyncStorage, Firebase, React Native

3. **APK собирается заново** - без minification

## 🔧 Следующие шаги

### 1. Дождаться окончания сборки
Сборка идет, просто долго (3-4 минуты). Подожди завершения.

### 2. Установить новый APK
```bash
cd bakery-mobile-app/bakery-app/android
adb install app/build/outputs/apk/release/app-release.apk
```

### 3. Протестировать вход/регистрацию
- Открыть приложение
- Нажать "Войти" или "Создать аккаунт"
- Проверить, что приложение НЕ крашится

### 4. Если все работает
Отлично! Приложение работает без minification.

### 5. Если краш продолжается
Нужно получить логи:

```bash
# Подключить устройство
adb devices

# Запустить logcat
adb logcat | findstr /i "crash error exception fatal"

# Или сохранить в файл
adb logcat > crash_log.txt
```

## 📊 Размер APK

Без minification APK будет больше (~80-90 MB вместо 72 MB).

Это нормально для отладки. Позже можно:
1. Включить minification обратно
2. Добавить правильные ProGuard правила
3. Уменьшить размер до 40-50 MB

## 🐛 Возможные причины краша (если продолжается)

1. **Supabase подключение** - проверить URL и ключ
2. **AsyncStorage** - проверить права доступа
3. **react-native-reanimated** - несовместимость с RN 0.81.5
4. **Навигация** - проблемы с expo-router

## 💡 Рекомендации

### Краткосрочные (сейчас)
- Использовать APK без minification для разработки
- Протестировать все функции приложения
- Собрать логи если краш продолжается

### Долгосрочные (потом)
- Обновить React Native до 0.76.x
- Обновить все зависимости
- Включить minification с правильными ProGuard правилами
- Настроить splits по архитектурам

## 🚀 Команды для сборки

### Debug сборка (для тестирования)
```bash
cd bakery-mobile-app/bakery-app/android
./gradlew assembleDebug
adb install app/build/outputs/apk/debug/app-debug.apk
```

### Release сборка (текущая)
```bash
cd bakery-mobile-app/bakery-app/android
./gradlew assembleRelease
adb install app/build/outputs/apk/release/app-release.apk
```

### Очистка перед сборкой
```bash
./gradlew clean
./gradlew assembleRelease
```

## 📝 Логи для отладки

Если нужно больше информации, добавь в `AuthContext.tsx`:

```typescript
console.log('🔐 Sign in attempt:', { email });
console.log('✅ Sign in success');
console.log('❌ Sign in error:', error);
```

И смотри логи через:
```bash
adb logcat | findstr /i "🔐 ✅ ❌"
```
