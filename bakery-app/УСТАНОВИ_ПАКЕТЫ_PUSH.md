# 📦 Установка пакетов для Push-уведомлений

## ⚠️ Важно!

Перед настройкой Firebase нужно установить пакеты.

---

## 🔧 Установка

### Вариант 1: Через bat файл
```bash
install-push-packages.bat
```

### Вариант 2: Вручную
```bash
cd bakery-app/bakery-mobile-app/bakery-app
npm install expo-device@~5.8.0 expo-application@~5.8.0
```

### Вариант 3: Через Expo CLI
```bash
cd bakery-app/bakery-mobile-app/bakery-app
npx expo install expo-device expo-application
```

---

## ✅ Проверка установки

После установки проверь `package.json`:

```json
{
  "dependencies": {
    "expo-device": "~5.8.0",
    "expo-application": "~5.8.0"
  }
}
```

---

## 🚀 Дальше

После установки пакетов:

1. Открой **НАЧНИ_ОТСЮДА_PUSH.md**
2. Следуй инструкциям в **БЫСТРЫЙ_СТАРТ_PUSH.md**

---

## 📝 Что делают эти пакеты

- **expo-device** - определяет информацию об устройстве (модель, бренд, ОС)
- **expo-application** - получает информацию о приложении (версия, ID)

Эти данные сохраняются вместе с FCM токеном для отладки.

---

## ❓ Проблемы с установкой?

### Ошибка "Cannot find module"
```bash
npm install
```

### Ошибка "Peer dependencies"
```bash
npm install --legacy-peer-deps
```

### Ошибка "Network timeout"
```bash
npm config set registry https://registry.npmjs.org/
npm install
```

---

## 🎯 После установки

Переходи к настройке Firebase! 🔥
