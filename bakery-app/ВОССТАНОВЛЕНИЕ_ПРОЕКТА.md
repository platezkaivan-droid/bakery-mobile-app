# Восстановление проекта - Пекарня

## ✅ Текущее состояние проекта

Проект **полностью восстановлен** и находится в рабочем состоянии.

### Структура проекта
```
bakery-app/bakery-mobile-app/bakery-app/
├── app/                    # Экраны приложения (Expo Router)
│   ├── (tabs)/            # Основные табы
│   │   ├── home.tsx       # Главная страница с товарами
│   │   ├── cart.tsx       # Корзина
│   │   ├── favorites.tsx  # Избранное
│   │   ├── orders.tsx     # Заказы
│   │   ├── profile.tsx    # Профиль
│   │   └── tracking.tsx   # Отслеживание доставки
│   ├── auth/              # Авторизация
│   │   ├── login.tsx      # Вход
│   │   └── register.tsx   # Регистрация
│   ├── _layout.tsx        # Корневой layout
│   └── index.tsx          # Welcome screen
├── src/
│   ├── components/        # Компоненты
│   ├── constants/         # Константы (цвета)
│   ├── context/           # Контексты (Auth, Cart, Favorites и т.д.)
│   └── lib/              # Библиотеки (Supabase)
└── assets/               # Изображения товаров
```

## 🔧 Решённые проблемы

### 1. Авторизация через Supabase ✅
**Проблема**: Сессия слетала при обновлении страницы

**Решение**:
- Использование `AsyncStorage` для хранения сессии
- Правильная инициализация в `src/lib/supabase.ts`
- Корректная проверка сессии в `AuthContext`
- Централизованная защита роутов в `app/_layout.tsx`

**Файлы**:
- `src/lib/supabase.ts` - настройка клиента
- `src/context/AuthContext.tsx` - управление авторизацией
- `app/_layout.tsx` - защита роутов

### 2. Навигация (Expo Router) ✅
**Проблема**: Ошибка "Attempted to navigate before mounting"

**Решение**:
- Убрана избыточная логика редиректа из `app/index.tsx`
- Централизованная навигация в корневом layout
- Использование `useRootNavigationState` для проверки готовности

**Файлы**:
- `app/index.tsx` - welcome screen без редиректов
- `app/_layout.tsx` - централизованная навигация

### 3. Картинки товаров ✅
**Проблема**: Картинки не отображались при загрузке из базы

**Решение**:
- Создан словарь `PRODUCT_IMAGES` с локальными изображениями
- Маппинг по ключам для каждого товара
- Все изображения находятся в `assets/products/`

**Файл**: `app/(tabs)/home.tsx`

### 4. Загрузка аватарок ✅
**Проблема**: ERR_SSL_PROTOCOL_ERROR при загрузке файлов в браузере

**Решение**:
- Использование Base64 для хранения аватарок
- Сохранение как текст в поле `avatar_url` таблицы `profiles`
- Обход проблем с антивирусом и SSL

**Файл**: `src/context/AuthContext.tsx`

## 📦 Установленные зависимости

```json
{
  "@supabase/supabase-js": "^2.38.4",
  "expo": "~50.0.0",
  "expo-router": "~3.4.0",
  "react-native": "0.73.2",
  "@react-navigation/native": "^6.1.9",
  "expo-linear-gradient": "~12.7.2",
  "react-native-maps": "1.8.0"
}
```

## 🗄️ База данных Supabase

### Таблицы:
1. **profiles** - профили пользователей
   - id (UUID)
   - email
   - full_name
   - phone
   - avatar_url (text) - Base64 изображение
   - bonus_points
   - loyalty_level

2. **products** - товары
   - id (UUID)
   - name
   - price
   - image_url
   - category_id
   - rating
   - description

3. **orders** - заказы
4. **favorites** - избранное
5. **cart** - корзина

### Настройки:
- URL: `https://qkyhwdmhkoizxjazwnti.supabase.co`
- Anon Key: настроен в `src/lib/supabase.ts`
- RLS политики: настроены для всех таблиц

## 🚀 Как запустить

### 1. Установка зависимостей
```bash
cd bakery-app/bakery-mobile-app/bakery-app
npm install
```

### 2. Запуск приложения
```bash
npx expo start
```

### 3. Открытие
- **Веб**: нажми `w` в терминале
- **Android**: нажми `a` (требуется эмулятор)
- **iOS**: нажми `i` (требуется симулятор, только на Mac)
- **Телефон**: установи Expo Go и отсканируй QR-код

## 🎨 Функционал

### Реализовано:
- ✅ Авторизация (вход/регистрация)
- ✅ Главная страница с каталогом (24 товара)
- ✅ Категории товаров
- ✅ Поиск товаров
- ✅ Stories (Новинки, Акции, Рецепты, Советы)
- ✅ Корзина с подсчётом
- ✅ Избранное
- ✅ Профиль пользователя
- ✅ Бонусная система
- ✅ Тёмная/светлая тема
- ✅ Уведомления

### В разработке:
- ⏳ Оформление заказа
- ⏳ Отслеживание доставки на карте
- ⏳ История заказов
- ⏳ Редактирование профиля

## 📝 Известные особенности

### Аватарки
Аватарки сохраняются как Base64 строки в базе данных (не через Supabase Storage). Это решение обходит проблемы с SSL на localhost при разработке.

### Товары
Товары используют локальные изображения из `assets/products/`. Для добавления нового товара:
1. Добавь изображение в `assets/products/`
2. Добавь ключ в `PRODUCT_IMAGES` в `home.tsx`
3. Добавь товар в массив `PRODUCTS`

### Навигация
Используется Expo Router (file-based routing). Структура файлов определяет роуты.

## 🔐 Безопасность

- Anon key Supabase находится в коде (это нормально для клиентских приложений)
- RLS политики защищают данные на уровне базы
- Пользователи видят только свои данные

## 📱 Тестирование

### Тестовый аккаунт:
- Email: `test@test.com`
- Password: `test123`

Или создай новый аккаунт через регистрацию.

## 🐛 Отладка

### Очистка кеша:
```bash
npx expo start -c
```

### Переустановка зависимостей:
```bash
rm -rf node_modules package-lock.json
npm install
```

### Проверка логов:
- Открой DevTools в браузере (F12)
- Или используй React Native Debugger

## 📚 Документация

- [Expo Router](https://docs.expo.dev/router/introduction/)
- [Supabase JS](https://supabase.com/docs/reference/javascript/introduction)
- [React Native](https://reactnative.dev/docs/getting-started)

## ✨ Следующие шаги

1. **Добавить оформление заказа**
   - Форма с адресом доставки
   - Выбор способа оплаты
   - Подтверждение заказа

2. **Реализовать карту доставки**
   - Интеграция react-native-maps
   - Отслеживание курьера в реальном времени

3. **История заказов**
   - Список прошлых заказов
   - Детали заказа
   - Повторный заказ

4. **Редактирование профиля**
   - Изменение имени, телефона
   - Загрузка аватарки (уже работает через Base64)
   - Смена пароля

---

**Дата восстановления**: 3 декабря 2025  
**Статус**: ✅ Полностью рабочий проект
