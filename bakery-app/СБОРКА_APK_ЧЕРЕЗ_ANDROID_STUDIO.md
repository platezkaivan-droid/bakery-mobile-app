# 📱 СБОРКА APK ЧЕРЕЗ ANDROID STUDIO - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ⚠️ ВАЖНО!
Expo проекты имеют особенности сборки. Нужно сначала подготовить проект, а потом собирать в Android Studio.

## 🔧 ШАГ 1: Подготовка проекта

### 1.1 Закрой Android Studio

### 1.2 Открой терминал и перейди в папку проекта
```bash
cd bakery-app\bakery-mobile-app\bakery-app
```

### 1.3 Пересоздай Android проект через Expo
```bash
npx expo prebuild --platform android --clean
```

Эта команда:
- Удалит старую папку android/
- Создаст новую с правильной конфигурацией
- Настроит все зависимости

**Время выполнения:** 2-3 минуты

## 🏗️ ШАГ 2: Сборка через командную строку

### 2.1 Собери APK через Gradle
```bash
cd android
.\gradlew.bat assembleDebug
cd ..
```

**Время выполнения:** 5-10 минут (первая сборка)

### 2.2 Найди готовый APK
```
android\app\build\outputs\apk\debug\app-debug.apk
```

## 🎯 ШАГ 3: Сборка через Android Studio (альтернатива)

### 3.1 Открой Android Studio

### 3.2 File → Open
Выбери папку: `bakery-app\bakery-mobile-app\bakery-app\android`

### 3.3 Дождись синхронизации Gradle
Внизу будет прогресс-бар "Gradle sync"

### 3.4 Build → Build Bundle(s) / APK(s) → Build APK(s)

### 3.5 Дождись завершения
Появится уведомление "APK(s) generated successfully"

### 3.6 Нажми "locate" в уведомлении
Откроется папка с APK

## 🚀 БЫСТРЫЙ СПОСОБ (РЕКОМЕНДУЕТСЯ)

Вместо всех этих шагов, просто выполни:

```bash
cd bakery-app\bakery-mobile-app\bakery-app
npx expo run:android
```

Это:
- Автоматически соберёт APK
- Установит на подключённое устройство
- Запустит приложение

**Требования:**
- Подключённый Android телефон с включённой "Отладка по USB"
- ИЛИ запущенный Android эмулятор

## 🔍 РЕШЕНИЕ ПРОБЛЕМ

### Ошибка: "SDK location not found"

**Решение 1:** Создай файл `android/local.properties`
```properties
sdk.dir=C:\\Users\\Administrator\\AppData\\Local\\Android\\Sdk
```

**Решение 2:** Установи переменную окружения
```bash
setx ANDROID_HOME "C:\Users\Administrator\AppData\Local\Android\Sdk"
```

### Ошибка: "Could not get unknown property 'release'"

Это ошибка Expo модулей. **Решение:**
```bash
npx expo prebuild --platform android --clean
```

### Ошибка: "react-native-maps:compileDebugJavaWithJavac failed"

**Решение:**
```bash
cd android
.\gradlew.bat clean
.\gradlew.bat assembleDebug --stacktrace
```

### Ошибка: "Execution failed for task"

**Решение:** Очисти кэш
```bash
cd android
.\gradlew.bat clean
.\gradlew.bat --stop
cd ..
rmdir /s /q android
npx expo prebuild --platform android
```

## 📝 ЧЕКЛИСТ ПЕРЕД СБОРКОЙ

- [ ] Node.js установлен
- [ ] npm install выполнен
- [ ] Android SDK установлен
- [ ] local.properties создан
- [ ] Expo CLI установлен (`npm install -g expo-cli`)

## 🎯 РЕКОМЕНДУЕМЫЙ ПОРЯДОК

### Для первой сборки:
```bash
# 1. Перейди в проект
cd bakery-app\bakery-mobile-app\bakery-app

# 2. Установи зависимости
npm install

# 3. Пересоздай Android проект
npx expo prebuild --platform android --clean

# 4. Собери APK
cd android
.\gradlew.bat assembleDebug
cd ..

# 5. Забери APK
# Он будет в: android\app\build\outputs\apk\debug\app-debug.apk
```

### Для последующих сборок:
```bash
cd bakery-app\bakery-mobile-app\bakery-app\android
.\gradlew.bat assembleDebug
```

## 📱 УСТАНОВКА APK НА ТЕЛЕФОН

### Способ 1: Через ADB
```bash
adb install android\app\build\outputs\apk\debug\app-debug.apk
```

### Способ 2: Вручную
1. Скопируй `app-debug.apk` на телефон
2. Открой файл на телефоне
3. Разреши установку из неизвестных источников
4. Нажми "Установить"

## 🎉 ГОТОВО!

После успешной сборки у тебя будет файл:
```
android\app\build\outputs\apk\debug\app-debug.apk
```

Размер: ~50-80 MB

Этот APK можно:
- Установить на любой Android телефон
- Отправить другим людям
- Загрузить в Google Play (после создания release версии)

---

**Следующий шаг:** Создание release версии для публикации в Google Play
