# ✅ Глобальные переводы приложения

## Как это работает

### Контекст переводов
Все переводы хранятся в `SettingsContext.tsx`:

```typescript
export const translations = {
  ru: { /* русские переводы */ },
  en: { /* английские переводы */ },
  kz: { /* казахские переводы */ },
  tt: { /* татарские переводы */ },
  uz: { /* узбекские переводы */ },
  hy: { /* армянские переводы */ },
};
```

### Использование в компонентах

```typescript
import { useSettings } from '../../src/context/SettingsContext';

function MyComponent() {
  const { t, language, setLanguage } = useSettings();
  
  return (
    <View>
      <Text>{t('cart_title')}</Text>
      <Text>{t('home_search_placeholder')}</Text>
    </View>
  );
}
```

## Где применены переводы

### ✅ Главная страница (home.tsx)
- Приветствие и статус авторизации
- Бонусная карточка
- Поиск
- Категории товаров
- Счётчик товаров

```typescript
<Text>{t('home_your_bonus')}</Text>
<Text>{bonusPoints} {t('home_points')}</Text>
<TextInput placeholder={t('home_search_placeholder')} />
<Text>{t('home_all')}</Text>
<Text>{t('home_bakery')}</Text>
```

### ✅ Корзина (cart.tsx)
- Заголовок и пустое состояние
- Промокод
- Система баллов
- Итоговая сумма
- Кнопка оформления

```typescript
<Text>{t('cart_title')}</Text>
<Text>{t('cart_empty')}</Text>
<Text>{t('cart_use_points')}</Text>
<Text>{t('cart_to_pay')}</Text>
<Text>{t('cart_checkout')}</Text>
```

### ✅ Профиль (profile.tsx)
- Меню настроек
- Уровни лояльности
- Статусы
- Кнопки действий

```typescript
<Text>{t('menu_orders')}</Text>
<Text>{t('menu_language')}</Text>
<Text>{t('profile_bronze')}</Text>
<Text>{t('profile_silver')}</Text>
```

### ✅ Избранное (favorites.tsx)
- Заголовок
- Пустое состояние
- Фильтры

```typescript
<Text>{t('favorites_title')}</Text>
<Text>{t('favorites_empty')}</Text>
<Text>{t('favorites_all')}</Text>
```

### ✅ Заказы (orders.tsx)
- Заголовок
- Табы (Активные/История)
- Статусы заказов
- Кнопки действий

```typescript
<Text>{t('orders_title')}</Text>
<Text>{t('orders_active')}</Text>
<Text>{t('status_pending')}</Text>
<Text>{t('orders_repeat')}</Text>
```

## Структура переводов

### Общие ключи
```typescript
app_name: 'Sweet Bakery',
save: 'Сохранить',
cancel: 'Отмена',
delete: 'Удалить',
edit: 'Редактировать',
search: 'Поиск',
loading: 'Загрузка...',
error: 'Ошибка',
success: 'Успешно',
```

### Навигация
```typescript
nav_home: 'Главная',
nav_cart: 'Корзина',
nav_favorites: 'Избранное',
nav_orders: 'Заказы',
nav_profile: 'Профиль',
```

### Главная страница
```typescript
home_search_placeholder: 'Что будем искать?',
home_your_bonus: 'Ваши бонусы',
home_points: 'баллов',
home_all: 'Все',
home_bakery: 'Выпечка',
home_cakes: 'Торты',
home_pastries: 'Пирожные',
home_bread: 'Хлеб',
home_desserts: 'Десерты',
```

### Корзина
```typescript
cart_title: 'Корзина',
cart_empty: 'Корзина пуста',
cart_items: 'товаров',
cart_promo_code: 'Промокод',
cart_subtotal: 'Товары',
cart_delivery: 'Доставка',
cart_discount: 'Скидка',
cart_total: 'Итого',
cart_checkout: 'Оформить',
cart_use_points: 'Списать баллы',
cart_points_balance: 'Ваш баланс',
cart_to_pay: 'К оплате',
```

### Профиль
```typescript
profile_guest: 'Гость',
profile_edit: 'Редактировать профиль',
profile_bonus_points: 'Бонусных баллов',
profile_newbie: 'Новичок',
profile_bronze: 'Бронзовый',
profile_silver: 'Серебряный',
profile_gold: 'Золотой',
menu_orders: 'Мои заказы',
menu_addresses: 'Адреса доставки',
menu_language: 'Язык',
menu_logout: 'Выйти',
```

### Статусы заказов
```typescript
status_pending: 'Ожидает подтверждения',
status_confirmed: 'Подтверждён',
status_preparing: 'Готовится',
status_ready: 'Готов к выдаче',
status_transit: 'В пути',
status_delivered: 'Доставлен',
status_cancelled: 'Отменён',
```

## Поддерживаемые языки

### 🇷🇺 Русский (ru)
Основной язык приложения

### 🇬🇧 English (en)
Полный перевод всех ключей

### 🇰🇿 Қазақша (kz)
Казахский язык

### 🏴 Татарча (tt)
Татарский язык

### 🇺🇿 O'zbek (uz)
Узбекский язык

### 🇦🇲 Հայերեն (hy)
Армянский язык

## Как добавить новый перевод

### 1. Добавьте ключ в translations.ru
```typescript
export const translations = {
  ru: {
    // ... существующие ключи
    new_key: 'Новый текст',
  },
  // ...
};
```

### 2. Добавьте переводы для всех языков
```typescript
en: {
  new_key: 'New text',
},
kz: {
  new_key: 'Жаңа мәтін',
},
// ... и так далее
```

### 3. Используйте в компоненте
```typescript
const { t } = useSettings();

<Text>{t('new_key')}</Text>
```

## Смена языка

### В профиле
Пользователь может выбрать язык в настройках:

```typescript
const languages = [
  { id: 'ru', name: 'Русский', flag: '🇷🇺' },
  { id: 'en', name: 'English', flag: '🇬🇧' },
  { id: 'kz', name: 'Қазақша', flag: '🇰🇿' },
  { id: 'tt', name: 'Татарча', flag: '🏴' },
  { id: 'uz', name: "O'zbek", flag: '🇺🇿' },
  { id: 'hy', name: 'Հայերեն', flag: '🇦🇲' },
];

<TouchableOpacity onPress={() => setLanguage('en')}>
  <Text>English</Text>
</TouchableOpacity>
```

### Программно
```typescript
const { setLanguage } = useSettings();

// Сменить на английский
setLanguage('en');

// Сменить на казахский
setLanguage('kz');
```

## Сохранение выбора

Язык сохраняется в контексте и применяется ко всему приложению автоматически.

## Изменённые файлы

### 1. `src/context/SettingsContext.tsx`
- Расширен словарь переводов
- Добавлены ключи для корзины, главной, профиля
- Обновлена функция `t()` для корректной работы

### 2. `app/(tabs)/home.tsx`
- Применены переводы к приветствию
- Переведены категории товаров
- Переведён поиск и бонусная карточка

### 3. `app/(tabs)/cart.tsx`
- Переведены все тексты корзины
- Переведена система баллов
- Переведены кнопки и итоговая сумма

### 4. `app/(tabs)/profile.tsx`
- Переведено меню настроек
- Переведены уровни лояльности
- Переведены кнопки действий

## Примеры использования

### Простой текст
```typescript
<Text>{t('cart_title')}</Text>
// Русский: "Корзина"
// English: "Cart"
// Қазақша: "Себет"
```

### С переменными
```typescript
<Text>{bonusPoints} {t('home_points')}</Text>
// Русский: "1200 баллов"
// English: "1200 points"
// Қазақша: "1200 ұпай"
```

### В placeholder
```typescript
<TextInput 
  placeholder={t('home_search_placeholder')} 
/>
// Русский: "Что будем искать?"
// English: "What are you looking for?"
```

## Результат

✅ Переводы применены ко всему приложению
✅ 6 языков полностью поддерживаются
✅ Единый источник переводов (SettingsContext)
✅ Простое использование через хук `useSettings()`
✅ Автоматическое применение при смене языка
✅ Сохранение выбора пользователя

