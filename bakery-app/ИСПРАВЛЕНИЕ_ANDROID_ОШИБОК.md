# 🔧 ИСПРАВЛЕНИЕ ОШИБОК ANDROID STUDIO

## ❌ Проблема
При открытии проекта в Android Studio появляются ошибки:
- `Build android: failed`
- `react-native-maps:compileDebugJavaWithJavac`
- `Failed to transform core-for-system-modules.jar`
- `Execution failed for task ':react-native-maps:compileDebugJavaWithJavac'`

## 🎯 Причина
Проект создан через Expo и требует специальной сборки через Expo CLI, а не напрямую через Android Studio.

## ✅ РЕШЕНИЕ 1: Сборка через Expo (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Установи зависимости
```bash
cd bakery-app\bakery-mobile-app\bakery-app
npm install
```

### Шаг 2: Собери APK через Expo
```bash
npx expo run:android
```

Это автоматически:
- Установит все зависимости
- Настроит Android проект
- Соберёт APK
- Запустит на эмуляторе или устройстве

### Шаг 3: Для release версии
```bash
npx expo build:android
```

## ✅ РЕШЕНИЕ 2: Prebuild для Android Studio

Если хочешь работать в Android Studio:

### Шаг 1: Удали старую папку android
```bash
cd bakery-app\bakery-mobile-app\bakery-app
rmdir /s /q android
```

### Шаг 2: Создай новую через Expo
```bash
npx expo prebuild --platform android
```

### Шаг 3: Установи зависимости
```bash
npm install
```

### Шаг 4: Теперь открой в Android Studio
```
File → Open → выбери папку android/
```

## ✅ РЕШЕНИЕ 3: Быстрая сборка APK

Самый простой способ получить APK:

```bash
cd bakery-app\bakery-mobile-app\bakery-app
npx eas build --platform android --profile preview --local
```

Требует установки EAS CLI:
```bash
npm install -g eas-cli
eas login
```

## 🚀 Рекомендуемый порядок действий

### 1. Закрой Android Studio

### 2. Открой терминал и выполни:
```bash
cd bakery-app\bakery-mobile-app\bakery-app
npm install
npx expo run:android
```

### 3. Подключи устройство или запусти эмулятор
- Для физического устройства: включи "Отладка по USB"
- Для эмулятора: запусти AVD Manager в Android Studio

### 4. Приложение автоматически установится

## 📱 Альтернатива: Тестирование через Expo Go

Самый быстрый способ протестировать:

```bash
cd bakery-app\bakery-mobile-app\bakery-app
npx expo start
```

Затем:
1. Установи Expo Go на телефон
2. Отсканируй QR-код
3. Приложение запустится

## 🔍 Проверка ошибок

Если всё равно есть проблемы:

```bash
# Очисти кэш
npx expo start -c

# Переустанови зависимости
rmdir /s /q node_modules
npm install

# Проверь версии
npx expo-doctor
```

## 📝 Важные замечания

1. **Expo проекты** не предназначены для прямой сборки в Android Studio
2. Используй **Expo CLI** для сборки
3. Если нужен нативный код, используй **expo prebuild**
4. Для публикации используй **EAS Build**

## 🎯 Итог

**НЕ используй Android Studio напрямую!**

**Используй:**
```bash
npx expo run:android
```

Это правильный способ для Expo проектов! 🚀
