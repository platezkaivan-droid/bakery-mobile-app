# 🔐 Полная настройка авторизации

## Что было сделано:

### ✅ 1. Нативный Google Sign-In
- Установлена библиотека `@react-native-google-signin/google-signin`
- Вход через нативное окно Android (без браузера!)
- Токен отправляется напрямую в Supabase

### ✅ 2. App Links для Email верификации
- Настроен AndroidManifest.xml с `autoVerify="true"`
- Создан файл `assetlinks.json` для GitHub Pages
- Ссылки из email открывают приложение напрямую

### ✅ 3. Улучшенный AuthContext
- Обработка всех типов токенов (OAuth, Email confirmation)
- Автоматическое сохранение FCM токена для push-уведомлений
- Запрос разрешений на уведомления (Android 13+)

---

## 🚀 Что нужно сделать:

### Шаг 1: Настройка Google Cloud Console

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите ваш проект (или создайте новый)
3. Перейдите в **APIs & Services → Credentials**

#### Создайте Android Client ID:
1. Нажмите **Create Credentials → OAuth client ID**
2. Выберите **Android**
3. Заполните:
   - **Name:** Bakery App Android
   - **Package name:** `com.bakery.pavlova`
   - **SHA-1 certificate fingerprint:** (см. ниже как получить)

#### Создайте Web Client ID:
1. Нажмите **Create Credentials → OAuth client ID**
2. Выберите **Web application**
3. **Name:** Bakery App Web
4. Скопируйте **Client ID** - он понадобится в коде!

### Шаг 2: Получение SHA-1 и SHA-256

```bash
# Debug keystore (для разработки)
cd android
keytool -list -v -keystore app/debug.keystore -alias androiddebugkey -storepass android

# Release keystore (для продакшена)
keytool -list -v -keystore YOUR_RELEASE.keystore -alias YOUR_ALIAS
```

Скопируйте:
- **SHA1** - для Google Cloud Console (Android Client ID)
- **SHA256** - для assetlinks.json

### Шаг 3: Настройка Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Перейдите в **Authentication → Providers → Google**
3. Включите **Enable Google**
4. Заполните:
   - **Client ID (for OAuth):** Web Client ID из Google Cloud
   - **Client Secret:** Client Secret из Google Cloud
   - **Authorized Client IDs (for Android):** Android Client ID
5. Включите **Skip nonce check** (рекомендуется)

### Шаг 4: Обновите код

Откройте `src/context/AuthContext.tsx` и замените:

```typescript
const GOOGLE_WEB_CLIENT_ID = 'YOUR_WEB_CLIENT_ID.apps.googleusercontent.com';
```

На ваш реальный Web Client ID из Google Cloud Console.

### Шаг 5: Загрузите assetlinks.json на GitHub Pages

1. Создайте репозиторий на GitHub (если еще нет)
2. Создайте папку `.well-known` в корне репозитория
3. Загрузите файл `assetlinks.json` в эту папку
4. Проверьте доступность: `https://YOUR-USERNAME.github.io/.well-known/assetlinks.json`

**Важно:** Файл должен быть доступен по HTTPS без редиректов!

### Шаг 6: Загрузите auth-callback.html

Загрузите файл `auth-callback.html` в корень вашего GitHub Pages репозитория.

### Шаг 7: Пересоберите приложение

```bash
cd android
./gradlew clean
./gradlew assembleRelease
```

---

## 📱 Как это работает:

### Google Sign-In (Нативный):
```
1. Пользователь нажимает "Войти через Google"
2. Открывается нативное окно Android с выбором аккаунта
3. Пользователь выбирает аккаунт
4. Приложение получает ID Token
5. Токен отправляется в Supabase
6. Supabase создает сессию
7. Пользователь авторизован! ✅
```

**Преимущества:**
- Нет перехода в браузер
- Мгновенный вход
- Работает стабильно

### Email верификация (App Links):
```
1. Пользователь регистрируется с email
2. Supabase отправляет письмо со ссылкой
3. Пользователь нажимает на ссылку в письме
4. Android видит домен github.io
5. Android проверяет assetlinks.json
6. Приложение открывается напрямую (без браузера!)
7. AuthContext обрабатывает токены
8. Пользователь авторизован! ✅
```

**Если assetlinks.json не настроен:**
```
1-3. То же самое
4. Открывается браузер с auth-callback.html
5. Страница пытается открыть приложение через Deep Link
6. Пользователь нажимает кнопку "Открыть приложение"
7. Приложение открывается
8. Пользователь авторизован! ✅
```

---

## 🧪 Тестирование:

### Тест 1: Google Sign-In
1. Откройте приложение
2. Нажмите "Войти через Google"
3. **Ожидаемый результат:** Нативное окно выбора аккаунта
4. Выберите аккаунт
5. **Ожидаемый результат:** Мгновенный вход без браузера

### Тест 2: Email регистрация
1. Откройте приложение
2. Нажмите "Регистрация"
3. Введите email и пароль
4. Проверьте почту
5. Нажмите на ссылку в письме
6. **Ожидаемый результат:** Приложение открывается и вы авторизованы

### Тест 3: App Links (проверка)
```bash
# Проверьте, что assetlinks.json доступен
curl https://platezkaivan-droid.github.io/.well-known/assetlinks.json

# Проверьте верификацию App Links на устройстве
adb shell pm get-app-links com.bakery.pavlova
```

---

## 🔧 Troubleshooting:

### Google Sign-In не работает:

1. **"DEVELOPER_ERROR"**
   - Проверьте SHA-1 в Google Cloud Console
   - Убедитесь, что package name = `com.bakery.pavlova`
   - Проверьте, что используете Web Client ID (не Android!)

2. **"SIGN_IN_CANCELLED"**
   - Пользователь отменил вход
   - Это нормальное поведение

3. **"PLAY_SERVICES_NOT_AVAILABLE"**
   - На устройстве нет Google Play Services
   - Используйте устройство с Google Play

### Email ссылка открывает браузер:

1. **assetlinks.json недоступен**
   - Проверьте URL: `https://YOUR-USERNAME.github.io/.well-known/assetlinks.json`
   - Файл должен отдаваться с `Content-Type: application/json`

2. **SHA-256 не совпадает**
   - Убедитесь, что в assetlinks.json правильный SHA-256
   - Для debug и release ключей SHA-256 разные!

3. **autoVerify не работает**
   - Переустановите приложение
   - Очистите кэш: `adb shell pm clear com.bakery.pavlova`

### Supabase не принимает токен:

1. **"Invalid token"**
   - Проверьте Client ID в Supabase
   - Убедитесь, что "Skip nonce check" включен

2. **"Provider not enabled"**
   - Включите Google в Supabase → Authentication → Providers

---

## 📁 Файлы для GitHub Pages:

Загрузите эти файлы в ваш репозиторий GitHub Pages:

```
your-repo/
├── .well-known/
│   └── assetlinks.json    # Для App Links
├── auth-callback.html     # Страница редиректа
└── index.html             # (опционально) главная страница
```

---

## ✅ Чек-лист:

- [ ] Создан Android Client ID в Google Cloud Console
- [ ] Создан Web Client ID в Google Cloud Console
- [ ] SHA-1 добавлен в Android Client ID
- [ ] Google включен в Supabase Providers
- [ ] Web Client ID добавлен в Supabase
- [ ] Android Client ID добавлен в Supabase (Authorized Client IDs)
- [ ] GOOGLE_WEB_CLIENT_ID обновлен в AuthContext.tsx
- [ ] assetlinks.json загружен на GitHub Pages
- [ ] auth-callback.html загружен на GitHub Pages
- [ ] Приложение пересобрано

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:

1. **Нативный Google Sign-In** - быстрый вход без браузера
2. **App Links** - email ссылки открывают приложение напрямую
3. **Push-уведомления** - FCM токен сохраняется автоматически
4. **Темная тема** - чат поддержки адаптируется под тему системы

Удачи! 🚀
