# 🔨 СБОРКА APK В ANDROID STUDIO

## ✅ Код уже обновлен!

Я изменил `emailRedirectTo` на GitHub Pages ссылку. Теперь нужно собрать APK в Android Studio.

---

## 🚀 ЧТО ДЕЛАТЬ СЕЙЧАС

### Шаг 1: Открой проект в Android Studio

1. Запусти **Android Studio**
2. Открой папку: `bakery-app/bakery-mobile-app/bakery-app/android`
3. Подожди, пока Gradle синхронизируется

### Шаг 2: Собери APK

1. В меню выбери: **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
2. Или нажми комбинацию: **Ctrl+Shift+A** → введи "Build APK"
3. Подожди 5-10 минут

### Шаг 3: Найди готовый APK

После сборки появится уведомление с кнопкой **locate**.

APK будет здесь:
```
bakery-app/bakery-mobile-app/bakery-app/android/app/build/outputs/apk/release/app-release.apk
```

---

## 🔄 Что изменилось в коде

**Файл:** `src/context/AuthContext.tsx`

**Было:**
```typescript
emailRedirectTo: 'bakery-app://auth-callback'
```

**Стало:**
```typescript
emailRedirectTo: 'https://platezkaivan-droid.github.io/bakery-email-redirect/'
```

---

## ⚠️ Если будут ошибки

### Ошибка: "SDK not found"
```bash
cd android
echo "sdk.dir=C:\\Users\\Administrator\\AppData\\Local\\Android\\Sdk" > local.properties
```

### Ошибка: "Gradle sync failed"
В Android Studio:
1. **File** → **Invalidate Caches / Restart**
2. Перезапусти Android Studio

### Ошибка: "Build failed"
Попробуй через командную строку:
```bash
cd bakery-app\bakery-mobile-app\bakery-app\android
gradlew assembleRelease
```

---

## 📱 После сборки

1. **Скопируй APK** на телефон
2. **Установи** (разреши установку из неизвестных источников)
3. **Протестируй:**
   - Email регистрацию → письмо → ссылка → приложение откроется!
   - Google OAuth → без зависаний
   - Админ-панель чата → все сообщения видны

---

## 🎯 Альтернатива: Сборка через Gradle (командная строка)

Если Android Studio тормозит, можешь собрать через терминал:

```bash
cd bakery-app\bakery-mobile-app\bakery-app\android
gradlew assembleRelease
```

APK появится в той же папке:
```
android/app/build/outputs/apk/release/app-release.apk
```

---

## 🔗 Что будет работать

После установки нового APK:
- ✅ Email-подтверждения через вечную GitHub Pages ссылку
- ✅ Google OAuth без зависаний
- ✅ Админ-панель чата с полным доступом
- ✅ Все функции приложения

---

## 💡 Совет

Если хочешь подписанный APK для Google Play:
1. **Build** → **Generate Signed Bundle / APK**
2. Выбери **APK**
3. Создай или выбери keystore
4. Заполни данные
5. Выбери **release**
6. Жди сборки

**Открывай Android Studio и собирай!** 🚀
