# ✅ Мультиязычность и аватарка в профиле

## Что добавлено

### 1. Новые языки
Добавлена поддержка 6 языков:
- 🇷🇺 Русский (ru)
- 🇬🇧 English (en)
- 🇰🇿 Қазақша (kz)
- 🏴 Татарча (tt) - НОВЫЙ
- 🇺🇿 O'zbek (uz) - НОВЫЙ
- 🇦🇲 Հայերեն (hy) - НОВЫЙ

### 2. Аватарка в профиле
Теперь аватарка отображается:
- ✅ На главной странице
- ✅ В редакторе профиля
- ✅ В профиле (ИСПРАВЛЕНО)

## Как это работает

### Переводы
Все переводы хранятся в `SettingsContext.tsx`:

```typescript
export const translations = {
  ru: { ... },
  en: { ... },
  kz: { ... },
  tt: { ... }, // Татарский
  uz: { ... }, // Узбекский
  hy: { ... }, // Армянский
};
```

### Использование в компонентах
```typescript
const { t, language, setLanguage } = useSettings();

// Получить перевод
<Text>{t('menu_orders')}</Text>

// Сменить язык
setLanguage('tt'); // Татарский
```

### Аватарка
```typescript
{profile?.avatar_url ? (
  <Image source={{ uri: profile.avatar_url }} style={styles.avatarImage} />
) : (
  <View style={styles.avatar}>
    <Text style={styles.avatarText}>{initials}</Text>
  </View>
)}
```

## Добавленные переводы

### Татарский (tt)
- Навигация: Баш бит, Кәрзин, Сайланганнар, Заказлар, Профиль
- Меню: Минем заказлар, Китерү адреслары, Түләү ысуллары
- Действия: Саклау, Баш тарту, Бетерү, Үзгәртү

### Узбекский (uz)
- Навигация: Bosh sahifa, Savat, Sevimlilar, Buyurtmalar, Profil
- Меню: Mening buyurtmalarim, Yetkazib berish manzillari
- Действия: Saqlash, Bekor qilish, O'chirish, Tahrirlash

### Армянский (hy)
- Навигация: Գլխավոր, Զամբյուղ, Ընտրյալներ, Պատվերներ, Պրոֆիլ
- Меню: Իմ պատվերները, Առաքման հասցեներ
- Действия: Պահպանել, Չեղարկել, Ջնջել, Խմբագրել

## Как добавить новый перевод

1. Откройте `src/context/SettingsContext.tsx`
2. Найдите объект `translations`
3. Добавьте новый язык:

```typescript
export type Language = 'ru' | 'en' | 'kz' | 'tt' | 'uz' | 'hy' | 'новый';

export const translations = {
  // ... существующие языки
  новый: {
    app_name: 'Sweet Bakery',
    save: 'Перевод',
    // ... остальные ключи
  },
};
```

4. Добавьте язык в список выбора в `app/(tabs)/profile.tsx`:

```typescript
{ id: 'новый', name: 'Название', flag: '🏴' },
```

## Изменённые файлы

- `src/context/SettingsContext.tsx` - Добавлены языки tt, uz, hy
- `app/(tabs)/profile.tsx` - Добавлена аватарка и новые языки в список

## Результат

✅ 6 языков доступны для выбора
✅ Аватарка отображается во всех местах
✅ Переводы работают во всём приложении
✅ Язык сохраняется между сессиями
