# 🔍 Отладка картинок в избранном

## Что сделано

Добавлен `console.log` в код избранного, который покажет:
- Название товара
- Ключ картинки из базы данных

## 📋 Инструкция по проверке

### Шаг 1: Откройте терминал с Expo

Найдите терминал где запущен `npx expo start`

### Шаг 2: Откройте избранное в приложении

Перейдите на вкладку "Избранное" в приложении

### Шаг 3: Смотрите логи в терминале

В терминале появятся строки вида:
```
🖼️ Товар: Эклер с кремом | Ключ картинки: eclair
🖼️ Товар: Штрудель яблочный | Ключ картинки: strudel
🖼️ Товар: Крем-брюле | Ключ картинки: cremeBrulee
```

---

## 🎯 Что проверить

### Если ключ картинки = `null` или `undefined`:
**Проблема**: В базе данных не заполнено поле `image_url`

**Решение**: 
1. Откройте Supabase → Table Editor → `products`
2. Найдите эти товары
3. Заполните колонку `image_url` правильными ключами

### Если ключ картинки на русском (например "эклер"):
**Проблема**: В базе данных русские названия, а в коде английские

**Решение**: Измените в базе данных на английские:
- `эклер` → `eclair`
- `штрудель` → `strudel`
- `крем-брюле` → `cremeBrulee`

### Если ключ картинки правильный, но картинка не показывается:
**Проблема**: Ключ в базе не совпадает с ключом в коде

**Пример**: 
- В базе: `creme_brulee` (с подчеркиванием)
- В коде: `cremeBrulee` (camelCase)

**Решение**: Приведите к одному формату

---

## 📊 Таблица соответствий

Вот правильные ключи для всех товаров:

| Товар | Ключ в коде | Файл |
|-------|-------------|------|
| Круассан с шоколадом | `croissant` | kruassan s chocaladom.jpg |
| Синнабон | `cinnabon` | sinabon.jpg |
| Круассан с миндалём | `almondCroissant` | kruassan s mendalem.jpg |
| Датская булочка | `danish` | datskaya bulochka.jpg |
| Пончик | `donut` | ponchik glazirovanniy.jpg |
| Булочка с корицей | `cinnamonBun` | bulka s koricey.jpg |
| Наполеон | `napoleon` | napoleon classic.jpg |
| Медовик | `medovik` | medovic.jpg |
| Красный бархат | `redVelvet` | red.png |
| Чизкейк | `cheesecake` | cheesecake new york.jpg |
| Шоколадный торт | `chocolateCake` | chcolade tort.jpg |
| **Эклер** | **`eclair`** | ekler s cremom.jpg |
| Макаронс | `macarons` | makarons assorti.jpg |
| Тирамису | `tiramisu` | tiramissu.jpg |
| Профитроли | `profiterole` | profitroli.jpg |
| Капкейк | `cupcake` | kapcake.jpg |
| Багет | `baguette` | baget.jpg |
| Чиабатта | `ciabatta` | chiabatta.jpg |
| Ржаной хлеб | `ryeBread` | hleb rzhanoy.jpg |
| Фокачча | `focaccia` | fokacha s razmarinom.jpg |
| Тарт с ягодами | `berryTart` | tart s yagodami.jpg |
| Панна котта | `pannaCotta` | panna kotta.jpg |
| **Крем-брюле** | **`cremeBrulee`** | krem brulle.jpg |
| **Штрудель** | **`strudel`** | shtrudel.jpg |

---

## 🛠️ Быстрое исправление в Supabase

Если нужно обновить сразу все товары, выполните этот SQL:

```sql
-- Обновляем ключи картинок для проблемных товаров
UPDATE products SET image_url = 'eclair' WHERE name LIKE '%клер%';
UPDATE products SET image_url = 'strudel' WHERE name LIKE '%трудель%';
UPDATE products SET image_url = 'cremeBrulee' WHERE name LIKE '%рем-брюле%';
UPDATE products SET image_url = 'pannaCotta' WHERE name LIKE '%анна%котта%';
UPDATE products SET image_url = 'berryTart' WHERE name LIKE '%арт%ягод%';
```

---

## ✅ После исправления

1. Перезагрузите приложение (нажмите `r` в терминале Expo)
2. Откройте избранное
3. Проверьте что все картинки отображаются правильно
4. Можете удалить `console.log` из кода если всё работает

---

## 📝 Дата создания
${new Date().toLocaleString('ru-RU')}
