# 📱 Установка APK на Android устройство

## Путь к APK
```
C:\bakery-app\app\build\outputs\apk\release\app-release-signed.apk
```

## Способ 1: Через ADB (быстро)

### Проверить подключение
```powershell
adb devices
```

Должно показать ваше устройство:
```
List of devices attached
XXXXXXXXXX      device
```

### Установить APK
```powershell
adb install "C:\bakery-app\app\build\outputs\apk\release\app-release-signed.apk"
```

### Если APK уже установлен (переустановка)
```powershell
adb install -r "C:\bakery-app\app\build\outputs\apk\release\app-release-signed.apk"
```

## Способ 2: Через USB кабель

1. **Скопировать APK на телефон**
   - Подключите телефон к компьютеру через USB
   - Скопируйте файл `app-release-signed.apk` в папку Downloads на телефоне

2. **Включить установку из неизвестных источников**
   - Настройки → Безопасность → Неизвестные источники (включить)
   - Или: Настройки → Приложения → Специальный доступ → Установка неизвестных приложений → Файлы → Разрешить

3. **Установить APK**
   - Откройте файловый менеджер на телефоне
   - Найдите файл `app-release-signed.apk` в Downloads
   - Нажмите на файл
   - Нажмите "Установить"

## Способ 3: Через облако (Google Drive, Dropbox)

1. Загрузите APK в облако
2. Откройте ссылку на телефоне
3. Скачайте файл
4. Установите (см. Способ 2, шаги 2-3)

## ⚠️ Возможные проблемы

### "App not installed"
- Удалите старую версию приложения
- Попробуйте переустановить: `adb install -r ...`

### "Parse error"
- APK поврежден, пересоберите
- Проверьте, что файл полностью скопирован

### "Installation blocked"
- Включите установку из неизвестных источников
- Проверьте антивирус на телефоне

## 🧪 После установки

1. Откройте приложение
2. Проверьте, что оно не крашится при запуске
3. Попробуйте войти или зарегистрироваться
4. Если краш - получите логи:
   ```powershell
   adb logcat | findstr /i "crash error exception fatal"
   ```

## 📝 Удаление приложения

### Через ADB
```powershell
adb uninstall com.bakery.pavlova
```

### На телефоне
Настройки → Приложения → Bakery App → Удалить
