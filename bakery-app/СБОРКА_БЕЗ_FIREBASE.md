# Сборка APK без Firebase (временно)

## Проблема
Конфликт между Firebase плагином и Expo модулями при сборке. Ошибки:
- `expo-module-gradle-plugin not found`
- `com.android.library not found`

## Решение
Временно убрали Firebase для успешной сборки APK:

### 1. Убрали из android/build.gradle
```groovy
// УБРАЛИ:
// classpath('com.google.gms:google-services:4.4.0')
```

### 2. Убрали из android/app/build.gradle
```groovy
// УБРАЛИ:
// apply plugin: 'com.google.gms.google-services'
```

### 3. Откатили settings.gradle
Убрали неправильный includeBuild для expo-modules-core.

## Что работает
✅ Expo модули через autolinking
✅ React Native
✅ Все функции приложения кроме Push уведомлений

## Что НЕ работает
❌ Firebase Cloud Messaging (Push уведомления при закрытом приложении)

## Push уведомления
Push уведомления **БУДУТ РАБОТАТЬ** когда приложение открыто, через Supabase Realtime.
Для Push при закрытом приложении нужно будет добавить Firebase позже.

## Статус
🔄 Сборка APK запущена без Firebase
📦 APK будет готов для тестирования всех функций кроме Push при закрытом приложении
