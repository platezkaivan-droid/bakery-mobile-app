# 🌍 Мультиязычность в приложении

## ✅ Что уже работает

Мультиязычность уже полностью реализована! Поддерживаются языки:
- 🇷🇺 Русский (ru)
- 🇬🇧 Английский (en)
- 🇰🇿 Казахский (kz)
- 🇹🇹 Татарский (tt)
- 🇺🇿 Узбекский (uz)
- 🇦🇲 Армянский (hy)

### Что переведено:
- ✅ Все кнопки и меню
- ✅ Заголовки экранов
- ✅ Сообщения и уведомления
- ✅ Статусы заказов
- ✅ Формы ввода
- ⚠️ Названия товаров (нужно добавить в БД)

---

## 🎯 Как использовать в коде

### 1. Перевод статического текста (кнопки, заголовки)

В любом компоненте используйте хук `useSettings`:

\`\`\`tsx
import { useSettings } from '../../src/context/SettingsContext';

export default function MyScreen() {
  const { t } = useSettings();
  
  return (
    <View>
      <Text>{t('cart_title')}</Text>  {/* Выведет "Корзина" или "Cart" */}
      <Button title={t('save')} />     {/* Выведет "Сохранить" или "Save" */}
    </View>
  );
}
\`\`\`

### 2. Перевод названий товаров

Используйте утилиту `getLocalizedProduct`:

\`\`\`tsx
import { useSettings } from '../../src/context/SettingsContext';
import { getLocalizedProduct } from '../../src/utils/getLocalizedProduct';

export default function ProductCard({ product }) {
  const { language } = useSettings();
  const localizedProduct = getLocalizedProduct(product, language);
  
  return (
    <View>
      <Text>{localizedProduct.name}</Text>
      <Text>{localizedProduct.description}</Text>
      <Text>{product.price} ₽</Text>  {/* Цена одна для всех */}
    </View>
  );
}
\`\`\`

### 3. Переключение языка

Язык переключается в профиле пользователя:

\`\`\`tsx
import { useSettings } from '../../src/context/SettingsContext';

export default function LanguageSelector() {
  const { language, setLanguage } = useSettings();
  
  return (
    <TouchableOpacity onPress={() => setLanguage('en')}>
      <Text>Switch to English</Text>
    </TouchableOpacity>
  );
}
\`\`\`

---

## 📊 Добавление переводов товаров в БД

### Шаг 1: Выполните SQL-скрипт

1. Откройте Supabase → SQL Editor
2. Откройте файл `ДОБАВЛЕНИЕ_ПЕРЕВОДОВ_ТОВАРОВ.sql`
3. Скопируйте и выполните весь код
4. Это добавит колонки для переводов

### Шаг 2: Заполните переводы

После выполнения скрипта у вас будут колонки:
- `name_en`, `description_en` - английский
- `name_kz`, `description_kz` - казахский
- `name_tt`, `description_tt` - татарский
- `name_uz`, `description_uz` - узбекский
- `name_hy`, `description_hy` - армянский

### Шаг 3: Обновите запросы к БД

В `FavoritesContext.tsx` и других местах где загружаются товары, добавьте новые колонки:

\`\`\`tsx
const { data: productsData, error: prodError } = await supabase
  .from('products')
  .select(\`
    id, 
    name, 
    description, 
    price, 
    image_url, 
    rating, 
    category_id,
    name_en,
    description_en,
    name_kz,
    description_kz,
    name_tt,
    description_tt,
    name_uz,
    description_uz,
    name_hy,
    description_hy
  \`)
  .in('id', productIds);
\`\`\`

---

## 🔑 Доступные ключи переводов

### Навигация
- `nav_home` - Главная
- `nav_cart` - Корзина
- `nav_favorites` - Избранное
- `nav_orders` - Заказы
- `nav_profile` - Профиль

### Корзина
- `cart_title` - Корзина
- `cart_empty` - Корзина пуста
- `cart_checkout` - Оформить
- `cart_subtotal` - Товары
- `cart_delivery` - Доставка
- `cart_total` - Итого
- `cart_bonus_earn` - Вы получите
- `cart_bonus_points` - баллов

### Избранное
- `favorites_title` - Избранное
- `favorites_empty` - Избранное пусто
- `favorites_all` - Все
- `favorites_available` - Доступные
- `favorites_unavailable` - Нет в наличии

### Заказы
- `orders_title` - Мои заказы
- `orders_active` - Активные
- `orders_history` - История
- `orders_repeat` - Повторить
- `orders_track` - Отследить
- `orders_cancel` - Отменить заказ

### Статусы заказов
- `status_pending` - Ожидает подтверждения
- `status_confirmed` - Подтверждён
- `status_preparing` - Готовится
- `status_ready` - Готов к выдаче
- `status_transit` - В пути
- `status_delivered` - Доставлен
- `status_cancelled` - Отменён

### Профиль
- `profile_guest` - Гость
- `profile_login` - Войти в аккаунт
- `profile_edit` - Редактировать профиль
- `profile_bonus_points` - Бонусных баллов
- `menu_orders` - Мои заказы
- `menu_addresses` - Адреса доставки
- `menu_payment` - Способы оплаты
- `menu_language` - Язык
- `menu_theme` - Тема
- `menu_logout` - Выйти

### Общие
- `save` - Сохранить
- `cancel` - Отмена
- `delete` - Удалить
- `edit` - Редактировать
- `add` - Добавить
- `confirm` - Подтвердить
- `back` - Назад
- `close` - Закрыть
- `search` - Поиск
- `loading` - Загрузка...
- `error` - Ошибка
- `success` - Успешно

Полный список смотрите в `src/context/SettingsContext.tsx`

---

## 📝 Добавление новых переводов

### 1. Добавьте ключ в SettingsContext.tsx

\`\`\`tsx
export const translations = {
  ru: {
    // ... существующие переводы
    my_new_key: 'Мой новый текст',
  },
  en: {
    // ... существующие переводы
    my_new_key: 'My new text',
  },
  kz: {
    // ... существующие переводы
    my_new_key: 'Менің жаңа мәтінім',
  },
  // ... остальные языки
};
\`\`\`

### 2. Используйте в коде

\`\`\`tsx
const { t } = useSettings();
<Text>{t('my_new_key')}</Text>
\`\`\`

---

## 🎨 Примеры использования

### Пример 1: Карточка товара

\`\`\`tsx
import { useSettings } from '../../src/context/SettingsContext';
import { getLocalizedProduct } from '../../src/utils/getLocalizedProduct';

function ProductCard({ product }) {
  const { language, t } = useSettings();
  const { name, description } = getLocalizedProduct(product, language);
  
  return (
    <View>
      <Image source={PRODUCT_IMAGES[product.image_url]} />
      <Text>{name}</Text>
      <Text>{description}</Text>
      <Text>{product.price} ₽</Text>
      <Button title={t('cart_checkout')} />
    </View>
  );
}
\`\`\`

### Пример 2: Список заказов

\`\`\`tsx
import { useSettings } from '../../src/context/SettingsContext';

function OrdersList({ orders }) {
  const { t } = useSettings();
  
  return (
    <View>
      <Text>{t('orders_title')}</Text>
      {orders.map(order => (
        <View key={order.id}>
          <Text>{t(\`status_\${order.status}\`)}</Text>
          <Button title={t('orders_repeat')} />
        </View>
      ))}
    </View>
  );
}
\`\`\`

---

## ✅ Чек-лист внедрения

- [x] Система переводов настроена
- [x] Все экраны используют функцию `t()`
- [x] Создана утилита для товаров
- [ ] Выполнен SQL-скрипт для добавления колонок
- [ ] Заполнены переводы товаров в БД
- [ ] Обновлены запросы к БД (добавлены новые колонки)
- [ ] Протестировано переключение языков

---

## 📅 Дата создания
${new Date().toLocaleString('ru-RU')}
