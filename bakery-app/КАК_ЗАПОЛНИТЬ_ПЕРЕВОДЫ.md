# 🌍 Как заполнить переводы товаров в БД

## Проблема
Код работает правильно, но переводы не отображаются, потому что колонки `name_en`, `name_kz` и т.д. в базе данных **пустые (NULL)**.

## Решение за 2 минуты

### Шаг 1: Откройте Supabase
1. Зайдите на https://supabase.com
2. Откройте ваш проект
3. Перейдите в **SQL Editor** (слева в меню)

### Шаг 2: Выполните SQL-скрипт
1. Откройте файл `ЗАПОЛНЕНИЕ_ПЕРЕВОДОВ_ВСЕХ_ТОВАРОВ.sql`
2. Скопируйте **весь** его содержимое
3. Вставьте в SQL Editor в Supabase
4. Нажмите **RUN** (или Ctrl+Enter)

### Шаг 3: Проверьте результат
Выполните проверочный запрос:
```sql
SELECT 
  name as "Русский",
  name_en as "English",
  name_kz as "Қазақша"
FROM products
LIMIT 5;
```

Вы должны увидеть:
```
Русский                    | English              | Қазақша
---------------------------|----------------------|-------------------------
Круассан с шоколадом       | Chocolate Croissant  | Шоколадты круассан
Синнабон классический      | Classic Cinnamon Roll| Классикалық дарқұрама
...
```

### Шаг 4: Перезапустите приложение
```bash
# В терминале нажмите 'r' для перезагрузки
r
```

### Шаг 5: Проверьте в приложении
1. Откройте приложение
2. Зайдите в **Профиль → Язык**
3. Выберите **English**
4. Вернитесь на главную страницу
5. **Названия товаров должны измениться!**

## Что делает SQL-скрипт?

Скрипт заполняет переводы для **всех 24 товаров** на **6 языках**:
- 🇬🇧 English (en)
- 🇰🇿 Қазақша (kz)
- 🇹🇯 Татарча (tt)
- 🇺🇿 O'zbek (uz)
- 🇦🇲 Հայերեն (hy)

Для каждого товара переводятся:
- `name_*` - название
- `description_*` - описание

## Примеры переводов

### Круассан с шоколадом
- 🇷🇺 Круассан с шоколадом
- 🇬🇧 Chocolate Croissant
- 🇰🇿 Шоколадты круассан
- 🇹🇯 Шоколадлы круассан
- 🇺🇿 Shokoladli kruassan
- 🇦🇲 Շոկոլադե կրուասան

### Наполеон классический
- 🇷🇺 Наполеон классический
- 🇬🇧 Napoleon Cake
- 🇰🇿 Классикалық Наполеон
- 🇹🇯 Классик Наполеон
- 🇺🇿 Klassik Napoleon
- 🇦🇲 Դասական Նապոլեոն

## Если переводы всё равно не работают

### Проверка 1: Колонки существуют?
```sql
SELECT column_name 
FROM information_schema.columns 
WHERE table_name = 'products' 
  AND column_name LIKE 'name_%';
```

Должны быть: `name_en`, `name_kz`, `name_tt`, `name_uz`, `name_hy`

Если их нет, выполните:
```sql
ALTER TABLE products 
ADD COLUMN IF NOT EXISTS name_en TEXT,
ADD COLUMN IF NOT EXISTS name_kz TEXT,
ADD COLUMN IF NOT EXISTS name_tt TEXT,
ADD COLUMN IF NOT EXISTS name_uz TEXT,
ADD COLUMN IF NOT EXISTS name_hy TEXT,
ADD COLUMN IF NOT EXISTS description_en TEXT,
ADD COLUMN IF NOT EXISTS description_kz TEXT,
ADD COLUMN IF NOT EXISTS description_tt TEXT,
ADD COLUMN IF NOT EXISTS description_uz TEXT,
ADD COLUMN IF NOT EXISTS description_hy TEXT;
```

### Проверка 2: Данные заполнены?
```sql
SELECT 
  COUNT(*) as total,
  COUNT(name_en) as with_english,
  COUNT(name_kz) as with_kazakh
FROM products;
```

Если `with_english = 0`, значит скрипт не выполнился. Попробуйте снова.

### Проверка 3: AsyncStorage работает?
Откройте консоль в приложении и выполните:
```javascript
import AsyncStorage from '@react-native-async-storage/async-storage';
AsyncStorage.getItem('app_language').then(console.log);
```

Должно вывести: `"en"` или `"kz"` (выбранный язык)

## Как добавить свой перевод

Если хотите изменить перевод конкретного товара:

```sql
UPDATE products 
SET 
  name_en = 'Your Custom Name',
  description_en = 'Your custom description'
WHERE name = 'Круассан с шоколадом';
```

## Готово! 🎉

После выполнения SQL-скрипта:
- ✅ Все 24 товара переведены на 6 языков
- ✅ Переводы отображаются при смене языка
- ✅ Язык сохраняется после перезагрузки
- ✅ Картинки работают стабильно
- ✅ Избранное работает стабильно

Теперь можете тестировать мультиязычность!
