# 🔧 РЕШЕНИЕ ПРОБЛЕМЫ ДЛИННЫХ ПУТЕЙ

## Проблема
```
ninja: error: Stat(...): Filename longer than 260 characters
```

## Решение 1: Включить длинные пути в Windows (РЕКОМЕНДУЕТСЯ)

### Через PowerShell (от имени Администратора):

```powershell
New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force
```

### Через Редактор реестра:

1. Win + R → `regedit`
2. Перейди: `HKLM\SYSTEM\CurrentControlSet\Control\FileSystem`
3. Создай DWORD `LongPathsEnabled` = 1
4. Перезагрузи компьютер

### Через Group Policy:

1. Win + R → `gpedit.msc`
2. Computer Configuration → Administrative Templates → System → Filesystem
3. Включи "Enable Win32 long paths"
4. Перезагрузи

## Решение 2: Переместить проект (БЫСТРОЕ)

Перемести проект в короткий путь:

```bash
# Из:
C:\Users\Administrator\Documents\bakery-app\bakery-mobile-app\bakery-app\

# В:
C:\bakery\
```

## После исправления:

1. Очисти кэш:
```bash
cd C:\bakery
rd /s /q android\.gradle
rd /s /q android\app\.cxx
rd /s /q android\app\build
```

2. Запусти сборку заново в Android Studio

## Проверь также google-services.json

Убедись что в Firebase Console добавлен package name: `com.bakery.pavlova`
