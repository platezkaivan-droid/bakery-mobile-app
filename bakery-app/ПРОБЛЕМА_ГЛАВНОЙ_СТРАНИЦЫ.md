# 🔍 ПРОБЛЕМА: Товары на главной не переводятся

## Что происходит
- ✅ В **избранном** товары переводятся на татарский и армянский
- ❌ На **главной** товары остаются на русском

## Причина
На главной странице используется **локальный массив PRODUCTS**, а в избранном - **данные из Supabase**.

### Главная страница (home.tsx):
```typescript
const PRODUCTS: Product[] = [
  { 
    id: '...', 
    name: 'Круассан с шоколадом',  // Русский
    name_en: 'Chocolate Croissant',  // Английский ✅
    name_kz: 'Шоколадты круассан',  // Казахский ✅
    name_uz: 'Shokoladli kruassan',  // Узбекский ✅
    // name_tt: НЕТ!  ❌
    // name_hy: НЕТ!  ❌
  },
  // ... 24 товара
];

// Используется локальный массив:
const filteredProducts = PRODUCTS.filter(...);
```

### Избранное (favorites.tsx):
```typescript
// Загружает из Supabase:
const { favorites } = useFavorites();

// В БД есть ВСЕ переводы:
// name_tt ✅
// name_hy ✅
```

---

## ✅ Решение 1: Добавить переводы в массив (быстро)

Добавить поля `name_tt`, `description_tt`, `name_hy`, `description_hy` в каждый товар.

### Что сделано:
- ✅ Добавлены переводы для первых 3 товаров
- ⏳ Осталось 21 товар

### Что нужно сделать:
1. Открыть файл `ПОЛНЫЙ_МАССИВ_PRODUCTS_С_ПЕРЕВОДАМИ.txt`
2. Скопировать весь массив
3. Заменить им массив PRODUCTS в `app/(tabs)/home.tsx`

**Время:** 2 минуты  
**Сложность:** Очень просто

---

## ✅ Решение 2: Загружать из Supabase (правильно)

Изменить главную страницу чтобы загружать товары из Supabase, как в избранном.

### Преимущества:
- ✅ Все переводы уже есть в БД
- ✅ Не нужно дублировать данные
- ✅ Легко добавлять новые товары
- ✅ Единый источник данных

### Недостатки:
- ⏳ Требует изменения кода
- ⏳ Нужно добавить загрузку

**Время:** 10-15 минут  
**Сложность:** Средне

---

## 🎯 Рекомендация

### Сейчас (быстро):
Используйте **Решение 1** - скопируйте готовый массив из файла `ПОЛНЫЙ_МАССИВ_PRODUCTS_С_ПЕРЕВОДАМИ.txt`.

### Потом (правильно):
Переделайте на **Решение 2** - загрузку из Supabase.

---

## 📝 Почему так получилось?

Изначально главная страница была сделана с локальным массивом для быстрой разработки. Когда добавили мультиязычность, переводы добавили только для английского, казахского и узбекского. Татарский и армянский добавили позже, но забыли обновить локальный массив.

В избранном же всегда использовались данные из Supabase, поэтому там все переводы работают.

---

## ✅ Что уже работает

### На главной:
- ✅ Первые 3 товара переводятся на татарский и армянский
- ⏳ Остальные 21 товар - на русском

### В избранном:
- ✅ Все товары переводятся на все языки

---

## 🔧 Быстрое исправление

1. Откройте файл: `ПОЛНЫЙ_МАССИВ_PRODUCTS_С_ПЕРЕВОДАМИ.txt`
2. Скопируйте весь массив (Ctrl+A, Ctrl+C)
3. Откройте файл: `app/(tabs)/home.tsx`
4. Найдите строку: `const PRODUCTS: Product[] = [`
5. Выделите весь массив до закрывающей `];`
6. Вставьте скопированный массив (Ctrl+V)
7. Сохраните файл (Ctrl+S)

**Готово!** Теперь все товары на главной будут переводиться.

---

**Статус:** ⏳ Частично исправлено (3 из 24 товаров)  
**Файл с решением:** `ПОЛНЫЙ_МАССИВ_PRODUCTS_С_ПЕРЕВОДАМИ.txt`  
**Время исправления:** 2 минуты
