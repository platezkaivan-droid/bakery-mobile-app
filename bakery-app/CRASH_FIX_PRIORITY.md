# 🎯 План исправления краша - ПРИОРИТЕТЫ

## 🔴 КРИТИЧНО - Сделать СЕЙЧАС

### Проблема: Firebase SHA-1 не настроен

**Ваш SHA-1:** `5E:8F:16:06:2E:A3:CD:2C:4A:0D:54:78:76:BA:A6:F3:8C:AB:F6:25`

**Что делать:**

1. **Откройте Firebase Console**
   - https://console.firebase.google.com/
   - Проект: `bakery-app-6452b`
   - Settings ⚙️ → Project Settings

2. **Добавьте SHA-1**
   - Найдите Android app: `com.bakery.pavlova`
   - Add fingerprint
   - Вставьте: `5E:8F:16:06:2E:A3:CD:2C:4A:0D:54:78:76:BA:A6:F3:8C:AB:F6:25`
   - Save

3. **Скачайте новый google-services.json**
   - Download google-services.json
   - Замените файл: `android/app/google-services.json`

4. **Пересоберите APK**
   ```bash
   cd bakery-mobile-app/bakery-app/android
   ./gradlew clean assembleRelease
   ```

5. **Установите**
   ```bash
   adb install app/build/outputs/apk/release/app-arm64-v8a-release.apk
   ```

**Время:** 5-10 минут  
**Вероятность исправления:** 90%

---

## 🟡 ВАЖНО - Если первое не помогло

### Получить логи краша

**Вариант 1: Найти ADB**
```powershell
# В PowerShell
Get-ChildItem -Path "C:\Users\Administrator\AppData\Local\Android\Sdk" -Filter "adb.exe" -Recurse

# Запустить
& "путь\к\adb.exe" logcat *:E
```

**Вариант 2: Android Studio**
- Откройте проект в Android Studio
- View → Tool Windows → Logcat
- Запустите приложение
- Вызовите краш
- Скопируйте логи с `FATAL EXCEPTION`

**Что искать в логах:**
- `java.lang.ClassNotFoundException` → ProGuard проблема
- `com.google.firebase` → Firebase проблема
- `io.supabase` → Supabase проблема
- `AsyncStorage` → Storage проблема

---

## 🟢 ОПЦИОНАЛЬНО - Дополнительная отладка

### Debug APK для сравнения

Соберите debug версию и проверьте, работает ли она:

```bash
cd bakery-mobile-app/bakery-app/android
./gradlew assembleDebug
adb install app/build/outputs/apk/debug/app-debug.apk
```

Если debug работает, а release нет → проблема в minification/ProGuard (но мы его отключили).

---

## 📋 Чеклист исправлений

### Уже сделано ✅
- [x] ProGuard правила добавлены
- [x] Minification отключен (`android.enableMinifyInReleaseBuilds=false`)
- [x] Shrink resources отключен
- [x] SHA-1 получен из debug.keystore
- [x] APK собран с splits по архитектурам

### Нужно сделать ❌
- [ ] Добавить SHA-1 в Firebase Console
- [ ] Скачать новый google-services.json
- [ ] Пересобрать APK
- [ ] Протестировать вход/регистрацию

### Если краш продолжается 🔍
- [ ] Получить логи через adb logcat
- [ ] Проверить конкретную ошибку
- [ ] Добавить больше логов в AuthContext.tsx
- [ ] Собрать debug APK для сравнения

---

## 🎓 Объяснение проблемы

### Почему крашится только в release?

1. **Firebase OAuth требует SHA-1**
   - Debug keystore имеет свой SHA-1
   - Release keystore имеет другой SHA-1
   - Firebase проверяет SHA-1 при OAuth
   - Если SHA-1 не зарегистрирован → отказ → краш

2. **В вашем случае:**
   - Release использует debug.keystore (строка 115 в build.gradle)
   - Но SHA-1 НЕ добавлен в Firebase
   - `oauth_client: []` в google-services.json подтверждает это

3. **Почему не работает обычный вход (email/password)?**
   - Возможно, приложение крашится при инициализации AuthContext
   - Или при попытке сохранить сессию в AsyncStorage
   - Логи покажут точную причину

---

## 🚀 Ожидаемый результат

После добавления SHA-1 в Firebase:

**google-services.json изменится:**
```json
"oauth_client": [
  {
    "client_id": "616728560840-xxxxxxxxxx.apps.googleusercontent.com",
    "client_type": 3
  }
]
```

**Приложение будет работать:**
- ✅ Вход через email/password
- ✅ Регистрация
- ✅ Вход через Google (откроется браузер)
- ✅ Навигация между экранами

---

## 📞 Если нужна помощь

Пришлите:
1. Логи из `adb logcat` (особенно строки с `FATAL EXCEPTION`)
2. Скриншот Firebase Console → Android app → SHA certificate fingerprints
3. Содержимое `oauth_client` из нового google-services.json

---

## 💡 Совет на будущее

Когда создадите production keystore:
1. Получите SHA-1: `keytool -list -v -keystore release.jks`
2. Добавьте в Firebase Console
3. Обновите build.gradle чтобы использовать release.jks
4. Пересоберите APK

**Сейчас используется debug.keystore для release - это нормально для разработки, но не для production!**
